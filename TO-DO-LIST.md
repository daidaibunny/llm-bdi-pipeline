# To Do List

This file is the active progress tracker for the domain-level lifted
AgentSpeak(L) library research line. Historical branch cleanup and completed
PDDL/DFA migration tasks have been compacted; future work should update this
file before claiming a milestone is complete.

## Current Research Target

Achievement goals are the current scope. Temporal extended goals remain the
final target, but the DFA layer should sit above a credible achievement-goal
library instead of generating query-specific ASL libraries.

Target pipeline:

```text
PDDL domain + training problems
→ external generalized-planning learner / traces / sketches
→ feature binding
→ Layer B atomic goal modules
→ Layer C goal dependency composer
→ lifted AgentSpeak(L) domain-level plan library
→ held-out validation
→ counterexample-guided refinement
```

## Compact Completed Summary

| Area | Current status |
| --- | --- |
| HTN/HDDL removal | Completed; current benchmark direction is PDDL-only. |
| DFA high-level restoration | Completed; DFA remains the future TEG controller, not the final low-level library generator. |
| Domain-level synthesis skeleton | Implemented with schema candidates, external sketch candidates, Clingo selection, bounded validation, and counterexample inputs. |
| External backend audit | Implemented for pinned learner-sketches, h-policy-learner, d2l, and MOOSE reproduction notes. |
| Resource safety | External learner commands are guarded by default; keep memory at or below 16 GiB unless explicitly approved. |
| No synthetic ASL names | Tests cover no `achieve_*`, `transition_*`, or `dfa_state` in generated domain-level ASL. |
| No production Blocksworld hardcoding | Tests scan `src/domain_level_planning` for Blocksworld-only special cases. |

## Decisions To Confirm Or Keep Stable

| ID | Decision | Current stance | Why it matters | Status |
| --- | --- | --- | --- | --- |
| D1 | Theoretical guarantee | Use bounded-class guarantee, not universal PDDL completeness. | Universal generalized planning from arbitrary PDDL is not a credible claim; the paper must define the expressible class. | Accepted; needs formal write-up. |
| D2 | Main method family | Use goal-conditioned modular policy sketches, informed by serialized width and policy reuse. | This aligns subgoal decomposition with ASL modules better than flat policies or MOOSE-style singleton regression. | Accepted; implementation still incomplete. |
| D3 | Goal representation | Keep read-only `goal_<predicate>` facts as problem/DFA goal descriptors. | A domain-level library must know the current instance goal without becoming query-specific. | Accepted; semantics need stronger tests and write-up. |
| D4 | Fast Downward role | Use classical planners only for synthesis evidence, traces, counterexamples, and validation. | Runtime full-trace planning would collapse the contribution into a planner wrapper. | Accepted. |
| D5 | TEG integration | Keep DFA as an upper-layer controller over the achievement-goal library. | The achievement-goal library should stay domain-level while DFA remains query-specific. | Accepted; later integration pending. |
| D6 | Negative and disjunctive goals | Do not silently support them until semantics are designed. | Current Layer B/C assumes positive conjunctive achievement goals. | Open design decision. |
| D7 | Object-specific DLPlan features | Reject them unless a principled lifting method is implemented. | Guessing object-specific bindings would break lifted domain-level claims. | Accepted; future method pending. |

## Current Gaps

| ID | Layer | Gap | Current implementation | Required improvement | Acceptance check | Status |
| --- | --- | --- | --- | --- | --- | --- |
| G1 | Theory | Bounded-class guarantee needs final paper prose, but the implementation contract is now explicit. | Reports expose a machine-readable architecture contract with guarantee, non-goals, accepted/open decisions, open gaps, and a bounded hypothesis-class contract covering feature language, module language, composer language, progress language, correctness scope, validation scope, and exclusions. | Convert the machine-readable hypothesis class into the paper method section and keep it aligned with future implementation changes. | A method section and machine-readable contract state assumptions and non-goals without ambiguity. | Partially done. |
| G2 | Layer B | Atomic module learning still lacks a full multi-strategy module learner. | PDDL add-effect rules, precondition subgoal preparation, external sketch candidates, bounded progress constraints, trace slicing, last-achiever marking, trace-justification of selected atomic rules, lifted anti-unified support patterns, recursive-module descent audits, repair-constraint binding diagnostics, per-selected-atomic-rule evidence verdicts, evidence-weighted primitive action costs that prefer trace-supported strategies over unobserved schema fallbacks, context-compatible primitive action strategy groups that choose one strategy among equivalent lifted contexts instead of requiring every action schema, strategy-group candidate comparison reports with selected/rejected status, verdicts, costs, trace support, and rejection reasons, paper-profile exclusion of schema action capabilities with no trace/external/repair support, and selected/output lifted-rule provenance manifests now exist and are reported in the architecture contract/evidence matrix. | Add a multi-strategy module learner on top of the trace slices, anti-unified patterns, recursion certificates, repair diagnostics, strategy comparison reports, and provenance manifests, and use it to rank or reject unsafe alternatives beyond the current context-compatible strategy grouping, evidence-backed paper-profile filter, and evidence-weighted cost heuristic. | From training traces and schemas, the system can justify each selected `+!P(...)` module and reject unsafe alternatives. | Partially done. |
| G3 | Layer C | Goal dependency composer remains the largest research gap. | Shared-object trace ordering evidence, schema causal-interference ordering for precondition-support and delete-threat relations, bounded state-coverage constraints, counterexample failure classification, primitive-precondition repair evidence, evidence-matrix counts, selected/output composer provenance manifests, per-rule selected/output composer evidence verdicts, and all-candidate composer evidence with selected/rejected status, verdicts, costs, and rejection reasons now exist and are reported in the architecture contract; schema-derived causal/delete-threat ordering capabilities are now selector requirements and deterministic ASL output prioritizes them before generic goal dispatch, so schema-only Blocksworld emits bottom-up `+!g` composer rules without traces or domain-specific logic; primitive-precondition repair constraints are consumed as selector hard groups when matching prepare rules exist; atomic-progress refinement constraints are consumed as selector hard groups when matching atomic action rules exist; missing-module failures that name a concrete failed subgoal are narrowed to a precise atomic-progress constraint for that subgoal; top-level `!g` missing-composer failures are classified as Layer C state-coverage refinements; recursive-loop and step-limit failures are separated as non-generative termination diagnostics; goal-bound primitive-precondition failures can synthesize safe Layer B prepare candidates when read-only goal facts bind all extra variables; explicit counterexample goal-ordering constraints synthesize Layer C composer candidates and selector hard groups; explicit counterexample state-coverage constraints synthesize or merge conservative goal-dispatch composer candidates and selector hard groups, with rejected binding diagnostics when lifted ordering, state-coverage, or repair evidence is not executable, references undeclared predicates, or uses wrong predicate arity; atomic-progress rejection reports now also expose PDDL producer actions for declared target predicates, distinguish declared-but-unproducible targets, and reject invalid or negative lifted goals before selector binding. | Extend ordering learning with richer final-goal causal structure and make failure classifications generate new composer/module candidates beyond the current explicit goal-ordering, state-coverage, and goal-bound primitive-precondition repair subsets. | Strong goal-dependency domains such as Blocksworld produce bottom-up composer rules without domain hardcoding. | Partially done. |
| G4 | Goal facts | `goal_<predicate>` semantics need stricter contract. | Goal facts are read-only inputs, appear only in contexts, renderer validation rejects them in initial beliefs, plan heads, body calls, actions, or belief updates, and contract reports now include every context descriptor mapped back to its PDDL predicate plus mutable-location/read-only status. | Define negative-goal representation before supporting it. | Tests prove goal facts are descriptors only and never compiled as primitive actions or mutable subgoals. | Partially done. |
| G5 | Feature binding | DLPlan binding remains conservative. | Recoverable predicate, forward/reverse role-count, goal-aligned concept/role equality, goal-aligned concept/role intersection, and nullary features bind only when the DLPlan feature kind matches the PDDL predicate arity; reverse role bindings preserve the declared PDDL predicate while flipping lifted argument order; object-specific, distance, and vocabulary/arity-mismatch features are rejected with distinct machine-readable reasons; every backend feature now has machine-readable accepted/rejected diagnostics with binding kind, operators, action-candidate count, action-candidate details, promoted effects, and rejection reason; synthesis tests assert unsupported paper-policy diagnostics are preserved in `paper_policy_audits`, not only aggregated in external-consumption summaries. | Expand principled lifted bindings beyond the current safe DLPlan fragment, especially object-specific or distance features. | Every external feature and rule has a compiled or rejected reason with no silent fallback. | Partially done. |
| G6 | External backends | Paper-code reuse is an audit pipeline, not yet the full learner. | learner-sketches policies can be parsed, audited, bound, used when safe, and summarized in a synthesis evidence matrix by source and layer, including forward/reverse binary role DLPlan features; `backend_audit_matrix()` now records each pinned backend's paper role, input artifacts, output artifacts, reusable evidence, failure modes, pin status, resource profile, and current Layer B/Layer C consumption role; external policy sources pass through a consumption gate before parsing, so h-policy-learner, d2l, and unknown backend artifacts stay audit-only with machine-readable blocking gaps until verified adapters exist; unified synthesis reports include that matrix plus per-run accepted/rejected source gates, ready policies, compiled/rejected rules, candidates, feature rejection reasons, and per-missing-rule-binding diagnostics; paper profile now treats rejected external policy sources as hard failures so mixed verified/audit-only paper artifacts cannot be silently ignored; paper policy audit readiness now requires every parsed learned rule to have bound conditions, bound effects, and a non-empty executable body; external learned sketch candidates must be selected by the Clingo synthesis step before appearing in output ASL, and paper profile fails if none are selected; the generic experiment runner can now forward external learned policies into bootstrap or paper-profile synthesis so report generation can exercise paper-backend evidence paths. | Implement verified policy-to-ASL adapters for non-learner-sketches backends before allowing them to drive synthesis. | A backend matrix and per-source gate report record input, output, reusable evidence, failure modes, resource profile, whether each backend currently drives Layer B/Layer C synthesis, and why audit-only sources were rejected. | Partially done. |
| G7 | ASL compiler | Compiler subset needs a full paper-level semantics contract. | Generated contexts/actions/subgoals are rendered and validated in tests; recursive atomic candidates need a structural descent certificate before compilation; the domain-level contract now reports the currently executable ASL subset: lifted declared PDDL predicate heads or zero-argument `+!g`, achievement-goal plan triggers only, atom/`not atom` context literals interpreted as implicit conjunction, declared PDDL action/subgoal-only bodies, PDDL schema name-and-arity checks, deterministic first-applicable execution, fixed-point goal termination, primitive-action precondition checking through the PDDL STRIPS simulator, and PDDL-to-ASL symbol mapping for sanitized primitive action functors; experiment generated-output audits now include plan-head subset validity in the `supported_asl_subset` summary. | Extend the contract if the compiler supports more Jason/AgentSpeak constructs, and mirror primitive-action precondition handling and action-symbol mapping in the paper method section. | Unsupported plan trigger kinds, unsupported context expressions, unsupported body step kinds, symbols outside the PDDL domain schema, wrong PDDL predicate/action arities, or colliding sanitized action symbols fail before output; deterministic execution and the supported body/context/head subset are documented by report. | Partially done. |
| G8 | Validation | Current validation is bounded and smoke-test oriented. | Bounded all-reachable-state checks, first-applicable execution, Blocksworld first-20, and non-Blocksworld Labworkflow goal-dependency experiment runners exist; experiment reports include coverage, generated ASL, generated-output audit, library size metrics, synthesis/evaluation runtime metrics, failure-reason counts, failed-problem details, step-count summaries, top-level Layer B and Layer C learning-audit summaries including schema causal, delete-threat, and trace-ordering candidate/selected counts, optional explicit ablation metadata, optional already-completed external baseline metadata without running hidden baselines, a pure already-run report comparison table helper with baseline rows, best-baseline summary, coverage deltas against the ASL library, and paper-profile readiness/schema-only markers, refinement convergence/constraint analysis, selected/output rule provenance manifests, a top-level paper-quality summary with paper-profile readiness, external-sketch selected/output counts, and blocking failures, and a machine-readable experiment protocol that states the bounded evaluation scope, synthesis profile, external policy count, no-runtime-planner condition, and absence of IPC-wide baseline claims in smoke reports; the Blocksworld first-20 script now labels the default run as `bootstrap_schema_only` and can accept explicit paper-profile/external-sketch ablation inputs; the Labworkflow dependency-refinement script now labels the default run as `bootstrap_counterexample_refinement` and accepts an explicit override label; schema-only bootstrap markers now exclude counterexample/refinement-generated runs. | Add broader experiment protocol: more IPC-style domains, actual ablations and externally comparable baseline tables, held-out scaling, and deeper failure analysis. | Blocksworld and at least one non-Blocksworld domain have reproducible experiment tables with coverage, library size, runtime, and failure summaries. | Partially done. |
| G9 | Counterexample refinement | Refinement hooks exist but are not a full learning loop. | Counterexample problem files add transition-progress and state-coverage constraints; held-out failures are classified into lifted Layer B/Layer C refinement records; primitive precondition failures now report failing primitive action, lifted missing goals, and lifted missing preconditions; primitive repair constraints are fed into later synthesis rounds and become selector hard groups when existing prepare candidates match; atomic-progress constraints are fed into selector hard groups when matching atomic action rules exist; the refinement loop now continues when a failed held-out problem yields new lifted constraints even if the problem file was already present in the counterexample set; missing-module failures such as `no applicable plan for !P(args)` are now narrowed to the failed atomic subgoal before lifting, instead of conflating all currently missing final goals; top-level `no applicable plan for !g` failures are classified as `counterexample_state_coverage` Layer C constraints and counted in refinement summaries; recursive-loop failures produce `counterexample_recursive_loop` diagnostics and step-limit failures produce `counterexample_nontermination` diagnostics, without being misrouted into ordering/progress candidate generation; unified synthesis reports expose recursive-loop/nontermination diagnostics, diagnostic group types, explicit `generative: false` markers, and non-generative reasons; refinement summaries now separate generative constraints from pure diagnostic constraints; goal-bound unmatched repairs synthesize new conservative Layer B prepare candidates; explicit goal-ordering constraints synthesize Layer C composer candidates; explicit state-coverage constraints synthesize or merge conservative Layer C goal-dispatch candidates; unsafe unmatched repair constraints, undeclared or wrong-arity repair predicates, undeclared or wrong-arity repair failing actions, invalid/undeclared/wrong-arity goal-ordering constraints, invalid/undeclared/wrong-arity state-coverage constraints, and invalid atomic-progress constraints are reported as rejected binding diagnostics with available capabilities/rejection reasons instead of guessed or silently ignored; wrong-arity atomic-progress diagnostics identify the offending predicates and list PDDL producer actions for declared target predicates; declared-but-unproducible atomic-progress targets are rejected with `unproducible_atomic_progress_predicate`; invalid or negative lifted atomic-progress goals are rejected with `invalid_atomic_progress_lifted_goal` before they can bind selector groups; paper-profile failure strings now surface atomic-progress producible/unproducible predicates and producer-action maps; negative precondition repairs are rejected explicitly rather than treated as positive achievement modules; refinement traces include a machine-readable summary of convergence, held-out failures, added counterexamples, repair constraints, state-coverage constraints, atomic-progress constraints, recursion/nontermination diagnostics, generative-vs-diagnostic counts, and constraints grouped by failure kind, target layer, and constraint type. | Connect more failure classes to generated candidate rules, and synthesize missing candidates when no existing rule matches a failure classification outside the current explicit goal-ordering, state-coverage, and goal-bound repair subsets. | A failed held-out problem can automatically refine the library and improve validation coverage, with report summaries showing what changed. | Partially done. |
| G10 | PDDL scope | Supported PDDL fragment must stay explicit and machine-readable. | `PDDLSupportReport` now serializes domain/problem files, declared requirements, supported and unsupported requirements, unsupported blocks, unsupported expression operators, unsupported-fragment diagnostics, fragment assumptions, compile status, PDDL-to-AgentSpeak predicate symbol compatibility, and action-symbol collision diagnostics after AgentSpeak sanitization; unified synthesis reports and library metadata include this audit before compilation proceeds. | Keep the report aligned with future fragment expansions, especially if negative goals, disjunctive goals, derived predicates, conditional effects, quantifiers, numeric fluents, or broader AgentSpeak predicate/action symbol escaping are ever intentionally supported. | Tests prove supported STRIPS inputs serialize cleanly, action-cost/numeric fragments are rejected with structured reasons and diagnostics, negative goals are rejected as outside the current positive-conjunctive achievement-goal fragment, PDDL predicate names outside the current AgentSpeak atom identifier subset are rejected before ASL generation, PDDL action names with IPC-style hyphens remain accepted as primitive body calls unless their sanitized AgentSpeak functors collide, and synthesis reports include the same PDDL audit. | Done for current fragment. |
| G11 | No-hardcoding | Audit should remain enforced as implementation grows. | Tests scan domain-level production code for domain-specific tokens and generated libraries; Blocksworld and Labworkflow experiments use the same generic runner; experiment reports now include a structured generated-output audit for synthetic names, grounded terms, initial beliefs, goal descriptor misuse, supported ASL subset compliance, declared PDDL schema provenance, selected/output rule manifest cleanliness, goal-descriptor usage, and learning-audit summaries. | Keep extending these checks whenever new modules or generated artifacts are added. | CI fails on domain-specific production branches or synthetic/non-lifted/generated non-PDDL-schema plan libraries, and experiment reports expose the same audit evidence. | Partially done. |
| G12 | TEG readiness | DFA-to-library interface is not yet fully integrated. | Positive conjunctive DFA guards can be adapted into schema-validated read-only `goal_<predicate>` facts and PDDL predicate subgoal calls for the domain-level library; undeclared or wrong-arity guard predicates are rejected before request generation; unsupported guard adaptations now produce structured accepted/rejected diagnostics with distinct negative, false, and disjunctive rejection reasons; raw disjunctive guards are rejected before event-to-PDDL mapping can partially accept one branch; the DFA controller now exposes per-progress-transition request diagnostics so supported and rejected outgoing transitions can be reported without executing them; a runtime DFA controller selects progress transitions and executes their requests through the same domain-level lifted ASL library without generating query-specific ASL; a temporal artifact pipeline persists the domain-level ASL library separately from query-specific DFA metadata, progress requests, and guard diagnostics; the runtime controller can execute repeated progress steps until an accepting DFA state, and earlier guard satisfaction is tracked by DFA state rather than protected in the world state; the ASL subset and execution semantics used by this library are now reported in the architecture contract. | Define negative/disjunctive guard semantics and run broader temporal-goal evaluation beyond smoke tests. | A DFA transition guard can call the same lifted library used for ordinary achievement goals. | Partially done. |

## Implementation Tasks

| ID | Task | Gap addressed | Acceptance check | Status |
| --- | --- | --- | --- | --- |
| T1 | Add machine-readable architecture contract and gap report to synthesis output. | G1, G4, G7, G10 | `UnifiedSynthesisResult.report` exposes guarantee, decisions, support boundaries, and open gaps. | Done |
| T2 | Add tests for the architecture contract report. | G1 | Tests assert bounded-class scope, `goal_*` decision, Layer B/C gap visibility, and no universal-PDDL claim. | Done |
| T3 | Strengthen goal-fact descriptor tests. | G4 | Tests assert goal facts remain problem/DFA descriptors and are rejected as initial beliefs, plan heads, body calls, primitive actions, or belief updates. | Done |
| T4 | Extend no-hardcoding and synthetic-name audit when new modules are added. | G11 | Tests cover source and generated domain-level libraries for domain-specific logic, synthetic names, grounded output, mutable goal descriptors, and initial beliefs. | Done |
| T5 | Add backend evidence matrix to reports. | G6 | Report separates schema, external sketch, trace evidence, and counterexample evidence by layer. | Done |
| T6 | Implement trace slicing and last-achiever evidence extraction for Layer B. | G2 | Training trace evidence can explain selected atomic rules beyond raw add-effect schema candidates. | Done |
| T7 | Implement lifted anti-unification for repeated atomic-goal examples. | G2 | Repeated grounded examples produce one lifted achievement pattern with support and last-achiever counts in the evidence matrix. | Done |
| T8 | Add recursion descent/ranking checks for recursive modules. | G2, G7 | Recursive module candidates must expose a missing-precondition descent certificate or a bounded acyclic-relation ranking certificate; unsupported recursion is rejected before ASL compilation. | Done |
| T9 | Implement causal-interference ordering candidates for Layer C. | G3 | Delete/precondition interactions generate candidate composer ordering constraints. | Done |
| T10 | Convert held-out execution failures into lifted refinement constraints. | G3, G9 | Failure classifier creates lifted Layer B/Layer C refinement records and counterexample required-rule-group summaries without polluting base training data. | Done |
| T11 | Build reproducible Blocksworld first-20 experiment report from the current library path. | G8 | Train/test split, generated ASL, coverage, failures, and no-hardcoding checks are reproducible with one command. | Done |
| T12 | Add at least one non-Blocksworld goal-dependency experiment. | G8, G11 | Demonstrates the approach is not tuned to Blocksworld. | Done |
| T13 | Define and test the DFA guard to achievement-goal request adapter. | G12 | DFA guard conjunctions call `!P(...)` subgoals through the domain-level library. | Done |
| T14 | Add machine-readable PDDL fragment audit to synthesis reports. | G10 | `PDDLSupportReport.to_dict()` and `UnifiedSynthesisResult.report["pddl_support"]` expose supported/rejected requirements, blocks, operators, positive-goal assumptions, and compile status. | Done |
| T15 | Add machine-readable ASL subset and execution semantics to contract reports. | G7, G12 | `DomainLevelLibraryContractReport.to_dict()` exposes supported heads, contexts, body steps, initial beliefs, deterministic plan selection, context negation semantics, and rejects unsupported body step kinds. | Done |
| T16 | Add structured generated-output audit to experiment reports. | G11 | Domain-level experiment JSON reports expose synthetic-name, grounded-term, initial-belief, goal-descriptor, and ASL-subset audit results derived from the domain-level contract. | Done |
| T17 | Add machine-readable external backend audit matrix. | G6 | `backend_audit_matrix()` and unified synthesis reports expose pinned backend status, paper role, input/output artifacts, reusable evidence, failure modes, and resource guard profile for learner-sketches, h-policy-learner, and d2l. | Done |
| T18 | Add counterexample-refinement summary reports. | G9 | `CounterexampleGuidedSynthesisResult.to_dict()` exposes convergence, round counts, held-out solved/failed counts, added counterexamples, and constraint counts grouped by failure kind, target layer, and type. | Done |
| T19 | Add machine-readable bounded hypothesis-class contract. | G1 | `ArchitectureContract.to_dict()` exposes the bounded feature, module, composer, progress, correctness, validation, and exclusion language used for the current generalized-planning claim. | Done |
| T20 | Add machine-readable feature-binding diagnostics. | G5, G6 | `FeatureBindingReport` and `PaperPolicyAuditReport.to_dict()` expose accepted/rejected diagnostics for each DLPlan feature, including binding kind, operators, action candidates, promoted primitive effects, and rejection reason; unified synthesis reports preserve the diagnostics through `paper_policy_audits`. | Done |
| T21 | Add lifted primitive-precondition repair evidence for held-out failures. | G3, G9 | `classify_heldout_failure_for_refinement(..., domain_file=...)` turns primitive precondition failures into Layer B repair constraints with lifted missing goals, failing primitive action, missing preconditions, and summary repair counts. | Done |
| T22 | Consume primitive-precondition repair constraints during synthesis. | G3, G9 | `synthesize_domain_level_asl_library(..., refinement_constraints=...)` maps `counterexample_atomic_precondition_repair` constraints onto matching lifted prepare-rule selector hard groups and reports the matched rule names and repair constraint counts. | Done |
| T23 | Report unmatched repair constraints without guessing. | G9 | Bootstrap synthesis records unmatched primitive-precondition repair constraints as rejected binding diagnostics; paper profile fails explicitly on unmatched repair evidence instead of silently claiming the failure was consumed. | Done |
| T24 | Add repair evidence to the Layer B evidence matrix. | G2, G9 | `UnifiedSynthesisResult.report["evidence_matrix"]["layer_b_atomic_modules"]` exposes repair constraint counts and matched/rejected repair binding reports beside trace, anti-unification, and recursion evidence. | Done |
| T25 | Keep architecture contract aligned with repair evidence. | G1, G2 | `ArchitectureContract.to_dict()` now states that Layer B evidence includes counterexample repair diagnostics and that the multi-strategy learner must use them. | Done |
| T26 | Classify unmatched repair rejection reasons. | G9 | Rejected primitive-precondition repair reports expose relevant available capabilities and distinguish unproducible precondition predicates from other missing lifted prepare-rule matches. | Done |
| T27 | Cover producible-but-unmatched repair rejection. | G9 | Tests verify primitive-precondition repair diagnostics distinguish producible missing preconditions with no matching lifted prepare rule from truly unproducible predicates. | Done |
| T28 | Synthesize safe goal-bound repair candidates. | G3, G9 | Counterexample primitive-precondition repairs generate a new Layer B prepare candidate only when read-only problem goal facts bind every extra lifted variable; unsafe free-variable repairs remain rejected. | Done |
| T29 | Synthesize explicit goal-ordering composer candidates. | G3, G9 | Counterexample goal-ordering refinements directly generate and select lifted `+!g` composer candidates and selector groups; richer composer invention remains future work. | Done |
| T30 | Report rejected goal-ordering refinements. | G3, G9 | Invalid explicit goal-ordering refinements produce machine-readable rejected binding diagnostics and fail the paper profile instead of being silently skipped. | Done |
| T31 | Reject undeclared goal-ordering predicates. | G3, G9 | Explicit goal-ordering refinements that reference predicates outside the PDDL domain are rejected with diagnostics and cannot generate non-PDDL ASL subgoals. | Done |
| T32 | Reject undeclared repair predicates. | G3, G9 | Primitive-precondition repair refinements that reference target or precondition predicates outside the PDDL domain are rejected with diagnostics and cannot generate non-PDDL ASL heads or body subgoals. | Done |
| T33 | Enforce declared PDDL symbols in final ASL contract. | G7, G11 | Domain-level contract and experiment audits reject generated ASL heads, contexts, body subgoals, or primitive actions whose symbols are not declared by the PDDL domain schema. | Done |
| T34 | Enforce PDDL schema arities in final ASL contract. | G7, G11 | Domain-level contract rejects generated ASL heads, contexts, body subgoals, or primitive actions whose argument counts do not match the declared PDDL predicate/action schema. | Done |
| T35 | Reject wrong-arity refinement predicates before candidate generation. | G3, G9 | Goal-ordering and primitive-precondition repair refinements with lifted atoms whose arity does not match the PDDL predicate schema are rejected with diagnostics and cannot generate invalid candidate rules. | Done |
| T36 | Reject invalid repair failing actions before binding. | G9 | Primitive-precondition repair refinements whose failing action is undeclared or has the wrong action-schema arity are rejected with diagnostics before selector groups or repair candidates are generated. | Done |
| T37 | Validate DFA guard predicates against the PDDL schema. | G12 | `adapt_dfa_guard_to_achievement_request(...)` accepts `domain_file` or declared predicates and rejects undeclared or wrong-arity guard atoms before emitting `goal_<predicate>` facts or PDDL predicate subgoal calls. | Done |
| T38 | Add a runtime DFA controller over the domain-level library. | G12 | `execute_dfa_progress_step(...)` selects DFA progress transitions, converts positive guards into schema-validated achievement requests, and executes them through the existing lifted ASL library instead of generating query-specific transition ASL. | Done |
| T39 | Persist domain-level temporal artifacts without query-specific ASL. | G12 | `build-temporal-domain-artifact` writes the lifted domain-level ASL library, synthesis report, DFA metadata, and schema-validated DFA progress requests as separate artifacts; generated ASL remains free of `achieve_*`, `transition_*`, and `dfa_state`. | Done |
| T40 | Execute DFA temporal controllers across multiple progress steps. | G12 | `execute_dfa_until_accepting(...)` repeatedly routes DFA guards through the same domain-level lifted ASL library until acceptance, without protecting earlier satisfied guards in the final world state. | Done |
| T41 | Consume atomic-progress refinement constraints. | G3, G9 | `counterexample_atomic_progress` refinements become selector hard groups when declared predicates have matching atomic action rules, and invalid or unmatched constraints are reported without generating guessed rules. | Done |
| T42 | Classify unsupported DLPlan feature-binding reasons. | G5, G6 | Object-specific features, distance features, and recoverable-shape vocabulary mismatches now produce distinct machine-readable rejection reasons instead of one generic unsupported-feature reason. | Done |
| T43 | Report wrong-arity atomic-progress predicates. | G9, G11 | Wrong-arity `counterexample_atomic_progress` constraints now include `wrong_arity_predicates` in machine-readable rejected diagnostics. | Done |
| T44 | Align architecture contract with current diagnostics. | G1, G5, G9 | The machine-readable architecture contract now documents distinct object-specific/distance/vocabulary feature-binding rejections and wrong-arity atomic-progress diagnostics. | Done |
| T45 | Reject negative precondition repairs explicitly. | G9 | `counterexample_atomic_precondition_repair` constraints with `not P(...)` preconditions are reported as `negative_repair_precondition_unsupported` and are not matched to positive achievement-module capabilities. | Done |
| T46 | Include repair rejection reasons in paper-profile failures. | G9 | Paper-profile failure messages for unmatched primitive-precondition repairs now include the machine-readable rejection reason, including negative repair rejections. | Done |
| T47 | Expose backend consumption roles. | G6 | `backend_audit_matrix()` now states whether each pinned external backend currently drives Layer B/Layer C synthesis, how it is consumed, and what adapter gap blocks audit-only backends. | Done |
| T48 | Add experiment size and runtime metrics. | G8 | Domain-level experiment reports now include primitive-action/subgoal/ASL-line counts plus synthesis and per-problem evaluation runtimes. | Done |
| T49 | Assert experiment metrics in script-level reports. | G8 | Blocksworld and Labworkflow script tests assert the generated JSON reports include library size metrics plus synthesis and per-problem evaluation runtimes. | Done |
| T50 | Align architecture contract with current gap model. | G1, G6, G7, G8, G9, G10, G11, G12 | Unified synthesis reports now expose the current G1-G12 gap taxonomy, D7 object-specific feature decision, and gap statuses matching the active TODO contract. | Done |
| T51 | Add experiment failure-analysis summaries. | G8 | Domain-level experiment reports now include failed problem details, failure-reason counts, and evaluation step-count summaries; unit and script tests assert the fields. | Done |
| T52 | Add structured DFA guard adaptation diagnostics. | G12 | DFA guard adaptation now exposes accepted/rejected diagnostics; temporal artifacts persist DFA progress diagnostics while keeping unsupported negative/disjunctive guards rejected rather than silently compiled. | Done |
| T53 | Add selected/output rule provenance manifests. | G2, G3, G8 | Unified synthesis reports now expose selected and output lifted-rule manifests with source, rationale, head, context, body, capabilities, and cost for auditability. | Done |
| T54 | Add structured PDDL unsupported-fragment diagnostics. | G10 | PDDL support reports now expose machine-readable unsupported requirement, block, expression-operator, and goal-fragment diagnostics in addition to legacy reason strings. | Done |
| T55 | Add rule manifest cleanliness audit. | G11 | Unified synthesis reports now audit selected/output rule manifests for synthetic names and grounded terms so intermediate report artifacts stay aligned with final ASL cleanliness checks. | Done |
| T56 | Align TODO gap descriptions with recent report evidence. | G2, G3, G8, G10, G11, G12 | Current gap rows now mention rule provenance manifests, failure-analysis summaries, structured PDDL diagnostics, manifest audits, and DFA guard diagnostics. | Done |
| T57 | Narrow missing-module failures to the failed atomic subgoal. | G3, G9 | `no applicable plan for !P(args)` counterexamples now produce precise `counterexample_atomic_progress` constraints for `P(args)` when that atom is one of the missing goals; broader missing-goal fallback remains unchanged. | Done |
| T58 | Classify top-level composer coverage failures separately. | G3, G9 | `no applicable plan for !g` counterexamples now produce Layer C `counterexample_state_coverage` constraints and refinement summaries expose state-coverage and atomic-progress constraint counts separately. | Done |
| T59 | Separate recursive-loop and nontermination diagnostics. | G3, G9 | `recursive loop on !P(args)` counterexamples now become `counterexample_recursive_loop` diagnostics for the relevant layer, and step-limit failures become `counterexample_nontermination` diagnostics before ordering/progress refinement can misclassify them. | Done |
| T60 | Expose primitive-action precondition semantics in the ASL contract. | G7, G9 | Domain-level contract reports that PDDL primitive actions are applied by the STRIPS simulator and precondition violations produce primitive-precondition counterexamples. | Done |
| T61 | Add top-level refinement analysis to experiment reports. | G8, G9 | Experiment reports now expose whether refinement ran, convergence, round count, constraint count, constraint counts by type/failure/layer, and first/final failed held-out counts without requiring consumers to parse nested traces. | Done |
| T62 | Enforce DLPlan concept/role/nullary arity during feature binding. | G5, G6 | Role features such as `r_primitive(P,0,1)` are rejected unless `P` is binary, concept features require unary predicates, and nullary booleans require zero-arity predicates. | Done |
| T63 | Summarize per-run external backend consumption. | G6, G8 | Unified synthesis reports now summarize external policy readiness, compiled and rejected rules, emitted candidates, and feature rejection reasons per policy, separate from the static backend audit matrix. | Done |
| T64 | Add per-rule Layer B evidence verdicts. | G2, G8 | The Layer B evidence matrix now lists every selected atomic rule with source, trace support count, action-body flag, capabilities, and a verdict such as `trace_justified`, `schema_no_action_body`, `external_policy_bound`, or `counterexample_repair_synthesized`. | Done |
| T65 | Enforce paper-profile Layer B action evidence. | G2 | Bootstrap `paper_profile_ready` rejects selected schema action atomic rules whose evidence verdict is `schema_unobserved_action_body`; `synthesis_profile="paper"` excludes unsupported schema action capabilities before selection and reports the excluded capabilities. | Done |
| T66 | Add per-rule Layer C composer evidence verdicts. | G3, G8 | The evidence matrix now reports selected and output composer rules with source, context, body, capabilities, and verdicts such as `schema_goal_dispatch`, `schema_causal_ordering`, `trace_ordering`, `counterexample_goal_ordering_synthesized`, and `external_policy_bound`. | Done |
| T67 | Add schema delete-threat goal ordering. | G3 | Layer C schema causal-interference rules now include domain-agnostic delete-threat orderings when achieving one goal deletes another goal literal over shared lifted arguments. | Done |
| T68 | Expose termination refinement diagnostics in synthesis reports. | G9 | Unified synthesis reports now count recursive-loop and nontermination refinement constraints and preserve their lifted missing goals, target layer, failure reason, and diagnostic group types. | Done |
| T69 | Bind goal-aligned concept intersection DLPlan features. | G5, G6 | `n_count(c_and(c_primitive(P,0),c_primitive(P_g,0)))` now binds conservatively for unary PDDL predicates with diagnostics as `goal_aligned_concept_intersection_count`. | Done |
| T70 | Gate external backend policy sources before synthesis consumption. | G6 | `ExternalSketchPolicySource` now records a backend name; learner-sketches remains the only currently consumable external backend, while h-policy-learner, d2l, and unknown backends are rejected before parsing with source-gate reports and blocking-gap diagnostics. | Done |
| T71 | Synthesize composer candidates from explicit state-coverage refinements. | G3, G9 | `counterexample_state_coverage` constraints now generate or merge conservative `+!g` goal-dispatch composer candidates, bind selector hard groups, and report matched/rejected state-coverage diagnostics in refinement and Layer C evidence summaries. | Done |
| T72 | Add evidence-weighted costs for primitive action strategies. | G2 | Schema-derived atomic action rules justified by training trace slices now receive lower selector cost than unobserved schema action fallbacks, and Layer B evidence reports count evidence-weighted versus unobserved action rules. | Done |
| T73 | Select among context-compatible primitive action strategies. | G2 | Atomic action rules with the same lifted head and same lifted context are grouped so Clingo selects one evidence-weighted strategy instead of requiring every action schema; Blocksworld keeps distinct pickup/unstack and stack/putdown contexts and still solves the first-20 smoke tests. | Done |
| T74 | Reject PDDL predicate symbols outside the current AgentSpeak atom identifier subset. | G10 | PDDL predicate names containing unsupported AgentSpeak atom identifier characters are now rejected by `PDDLSupportReport` with `unsupported_asl_symbol` diagnostics before ASL generation or final library contract checks; IPC-style hyphenated action names remain accepted as primitive body calls. | Done |
| T75 | Assert generated-output audit violation details in experiment reports. | G8, G11 | Unit and script-level experiment tests now require `generated_output_audit.violations == []` and expose the checked Layer contract keys, so reports are explainable instead of only pass/fail summaries. | Done |
| T76 | Fail paper profile on rejected external policy sources. | G6 | Paper-profile synthesis now rejects any external learned policy artifact blocked by the backend consumption gate, even if another learner-sketches policy compiles successfully; tests cover mixed verified and audit-only sources. | Done |
| T77 | Reject non-achievement AgentSpeak plan triggers in domain-level contracts. | G7, G11 | Domain-level contract reports a `plan_head_subset` check and rejects belief-trigger or other non-achievement plans before output; experiment audits assert the checked layer is present. | Done |
| T78 | Expose bounded experiment protocol in JSON reports. | G8 | Domain-level experiment reports now include a machine-readable protocol declaring bounded PDDL evaluation, no runtime planner, and empty baseline/ablation slots so smoke coverage cannot be mistaken for IPC-wide evidence. | Done |
| T79 | Distinguish generative and diagnostic refinement constraints. | G9 | Counterexample-refinement summaries now count constraints that can generate selector/candidate changes separately from recursion/nontermination diagnostics, so reports show whether a failure can actually refine the library. | Done |
| T80 | Expose DFA progress request diagnostics at controller level. | G12 | `inspect_progress_requests_from_dfa_state` reports supported and rejected outgoing progress transitions with schema/guard diagnostics, without changing strict execution behavior. | Done |
| T81 | Assert unsupported feature diagnostics in paper-policy audits. | G5, G6 | Tests now require unsupported external DLPlan features to keep full feature-level diagnostics in `paper_policy_audits`, including rejection reason and empty promoted-effect evidence. | Done |
| T82 | Include plan-head subset in generated-output ASL subset audit. | G7, G11 | `generated_output_audit.supported_asl_subset` now requires `plan_head_subset`, `body_step_subset`, and `context_subset`; tests cover non-achievement trigger summaries. | Done |
| T83 | Forward external paper policies through experiment reports. | G6, G8 | `run_domain_level_experiment` now accepts `external_sketch_policies` and `synthesis_profile`, forwards them through normal and refinement synthesis, and records profile/policy count in the experiment protocol; tests cover paper-profile report generation. | Done |
| T84 | Report Layer B primitive strategy comparisons. | G2, G8 | The Layer B evidence matrix now exposes `atomic_action_strategy_groups` with each candidate's selected status, evidence verdict, trace support, cost, body, and rejection reason; tests cover trace-supported strategy selection over an unobserved schema alternative. | Done |
| T85 | Report Layer C composer candidate evidence. | G3, G8 | The Layer C evidence matrix now exposes `composer_candidate_evidence` for every composer candidate with selected/rejected status, verdict, cost, body, and rejection reason; tests cover schema causal-ordering candidates without adding domain-specific logic. | Done |
| T86 | Add compact learning audit to experiment reports. | G8 | `run_domain_level_experiment` now exposes top-level `learning_audit` summaries for Layer B strategy candidates and Layer C composer candidates, including selected/rejected counts, verdict counts, and rejection counts; tests cover the report shape. | Done |
| T87 | Report goal descriptor usage in library contracts. | G4, G7 | `DomainLevelLibraryContractReport.to_dict()` now includes `goal_descriptor_usage` with context descriptors, mapped PDDL predicates, mutable locations, and read-only status; contract and experiment tests cover the field. | Done |
| T88 | Assert new audits in script-level experiment reports. | G8, G11 | Blocksworld and Labworkflow script tests now assert goal-descriptor usage and learning-audit summaries are present in reproducible JSON reports, preventing report-level regression. | Done |
| T89 | Explain non-generative termination refinements. | G9 | Recursive-loop and nontermination refinement diagnostics now include `generative: false` and a machine-readable `non_generative_reason`, so reports do not imply these failures already synthesize candidate rules. | Done |
| T90 | Reject unsupported DFA guard classes precisely. | G12 | DFA guard adaptation now distinguishes negative, false, and disjunctive guard rejections, and raw disjunctions are rejected before mapping so one branch cannot be silently accepted; tests cover each reason. | Done |
| T91 | Record explicit experiment ablation metadata. | G8 | `run_domain_level_experiment(..., ablation_label=...)` now records a machine-readable ablation entry with synthesis profile, external policy count, refinement flag, and no-runtime-planner marker without pretending to run an IPC baseline; tests cover default empty and explicit ablation metadata. | Done |
| T92 | Build ablation comparison tables from completed reports. | G8 | `compare_domain_level_experiment_reports(...)` now summarizes already-run experiment reports into rows with coverage, library size, profile, external-policy count, and refinement metadata, plus a best-by-coverage label; tests compare bootstrap and paper-profile reports without running hidden baselines. | Done |
| T93 | Select schema-derived goal-dependency composer rules. | G3 | Schema causal/delete-threat ordering capabilities are now selector requirements, and ASL output prioritizes ordering composer plans before generic `+!g` dispatch; schema-only Blocksworld tests prove bottom-up tower composer rules are emitted without trace evidence or domain-specific code. | Done |
| T94 | Surface schema ordering counts in experiment audits. | G3, G8 | Experiment `learning_audit.layer_c_goal_composer` now exposes causal-interference and delete-threat ordering candidate/selected counts; the Blocksworld script report test asserts both are present and selected. | Done |
| T95 | Record completed baseline metadata in experiment protocols. | G8 | `run_domain_level_experiment(..., baselines=...)` now records already-completed external baseline coverage metadata without running hidden planners, and `compare_domain_level_experiment_reports(...)` summarizes those baseline rows beside ASL-library ablations. | Done |
| T96 | Let new refinement constraints drive another synthesis round. | G9 | `synthesize_with_counterexample_refinement(...)` now accepts existing counterexample problem files and continues when a failed held-out evaluation produces new lifted constraints, even if no new problem file was added; the refinement summary reports constraint-only refinement rounds, and tests cover this control-flow case without domain-specific logic. | Done |
| T97 | Compare baseline coverage against the ASL library. | G8 | Baseline rows from `compare_domain_level_experiment_reports(...)` now include the corresponding ASL-library coverage and `coverage_delta_vs_library`, so already-completed external baselines can be reported without hidden planner runs. | Done |
| T98 | Summarize best completed baseline in comparison tables. | G8 | `compare_domain_level_experiment_reports(...)` now reports the best completed baseline label and its coverage delta against the corresponding ASL library, while returning `None` when no baseline rows exist. | Done |
| T99 | Surface trace-ordering counts in experiment audits. | G3, G8 | Experiment `learning_audit.layer_c_goal_composer` now exposes trace-ordering candidate/selected counts from the synthesis evidence matrix, and Blocksworld script reports assert selected trace-ordering evidence is visible beside schema ordering counts. | Done |
| T100 | Bind reverse binary DLPlan role features safely. | G5, G6 | `r_primitive(P,1,0)` and matching goal-aligned reverse role count/intersection features now bind only for declared binary PDDL predicates, compile to the same PDDL predicate with flipped lifted arguments, and preserve that order during goal-aligned action-candidate disambiguation. | Done |
| T101 | Cover reverse-role external sketch consumption. | G5, G6 | Unified synthesis now has an end-to-end test where a learner-sketches-style policy using `r_primitive(P,1,0)` is audited as executable, compiled into an external composer rule, and rendered without synthetic or domain-specific names. | Done |
| T102 | Expose atomic-progress producer diagnostics. | G9 | Rejected `counterexample_atomic_progress` diagnostics now include `producer_actions_by_predicate` and `producible_target_predicates`, derived only from declared PDDL add effects, so undeclared targets remain empty while wrong-arity declared targets can still report available producer actions. | Done |
| T103 | Distinguish unproducible atomic-progress targets. | G9 | Declared target predicates with no positive PDDL add-effect producers now reject as `unproducible_atomic_progress_predicate` and expose `unproducible_target_predicates`, instead of being conflated with selector-rule mismatches. | Done |
| T104 | Surface atomic-progress producer details in paper-profile failures. | G9 | Strict paper-profile failure messages for unmatched atomic-progress refinements now include producible targets, unproducible targets, and producer-action maps so report consumers do not need to inspect nested binding diagnostics. | Done |
| T105 | Reject invalid atomic-progress lifted goals. | G9 | Atomic-progress constraints now parse lifted goals with the same positive-atom parser used by repair/order refinements, reject malformed or negative targets as `invalid_atomic_progress_lifted_goal`, and record `invalid_lifted_goals` instead of binding them to selector groups. | Done |
| T106 | Label Blocksworld experiment ablations explicitly. | G8 | `run_blocksworld_first20_experiment.py` now records the default run as `bootstrap_schema_only` and exposes `--synthesis-profile`, repeated `--external-sketch-policy`, and `--ablation-label` so schema-only, paper-profile, and external-sketch runs are not conflated. | Done |
| T107 | Add paper-quality summaries to experiment reports. | G8 | `run_domain_level_experiment(...)` now exposes top-level paper-profile readiness, schema-only bootstrap status, external-policy count, and blocking failures so reports are not misread as paper-profile evidence when they are bootstrap ablations. | Done |
| T108 | Carry paper-quality markers into comparison tables. | G8 | `compare_domain_level_experiment_reports(...)` rows now include paper-profile readiness, schema-only bootstrap status, and paper blocking-failure counts beside coverage and library-size metrics. | Done |
| T109 | Label Labworkflow refinement ablations explicitly. | G8 | `run_labworkflow_dependency_experiment.py` now records the default run as `bootstrap_counterexample_refinement` and exposes `--ablation-label`; script tests assert both the default and overridden labels. | Done |
| T110 | Distinguish schema-only bootstrap from refinement bootstrap. | G8 | `paper_quality_summary.schema_only_bootstrap` is now false when the final synthesis used counterexample problems or refinement-generated candidates; Labworkflow refinement tests assert the corrected marker. | Done |
| T111 | Require learned policy rules to be executable before marking paper audit ready. | G6 | `PaperPolicyAuditReport.ready_for_executable_asl` now checks every parsed rule for bound condition operators, bound effect operators, and non-empty executable bodies; tests cover a policy whose features bind but whose rule condition cannot compile. | Done |
| T112 | Prevent unselected external learned sketches from bypassing synthesis. | G6, G8 | Output ASL now contains only selector-selected rules; paper profile fails when external learned candidates compile but none are selected, while bootstrap reports can still audit external candidates without emitting them. | Done |
| T113 | Surface external-sketch selected/output counts in experiment summaries. | G8 | `paper_quality_summary` and comparison rows now include selected and output external-sketch candidate counts, so bootstrap audits with external policies cannot be confused with paper-profile runs that actually select learned sketch rules. | Done |
| T114 | Assert external-sketch selection counts in script reports. | G8 | Blocksworld and Labworkflow CLI JSON report tests now require `paper_quality_summary.selected_external_sketch_candidate_count == 0` and `output_external_sketch_candidate_count == 0` for the current default bootstrap runs, keeping script outputs aligned with the paper-quality summary contract. | Done |
| T115 | Report selected external-sketch rule names. | G6, G8 | `paper_quality_summary` now includes selected and output external-sketch lifted rule names derived from the synthesis rule manifests; unit and script tests assert empty names for bootstrap runs and the concrete learned rule name for paper-profile smoke. | Done |
| T116 | Record PDDL-to-ASL symbol mapping. | G7, G8 | Unified synthesis and experiment reports now expose a `pddl_to_asl_symbol_map` for predicates, goal descriptors, and primitive actions, including changed action names such as PDDL `make-done` rendered as ASL `make_done`; tests cover the mapping in plan-library metadata, synthesis reports, experiment reports, and rendered ASL. | Done |
| T117 | Reject ASL action-symbol collisions. | G7, G10 | PDDL support inspection now rejects domains where distinct primitive action names collapse to the same rendered AgentSpeak functor after sanitization, while still allowing non-colliding hyphenated IPC-style action names; tests cover a `make-done`/`make_done` collision diagnostic. | Done |
| T118 | Prioritize primitive-precondition refinement. | G9 | Held-out failures whose reason is a primitive action precondition violation now generate Layer B atomic-precondition repair constraints before considering goal-ordering heuristics, even when another goal has already been satisfied; tests cover the previous misclassification as `goal_ordering_failure`. | Done |
| T119 | Explain ambiguous DLPlan action-effect candidates. | G5, G6 | Feature-binding diagnostics now include each action-effect candidate's feature id, DLPlan operator, affected predicate, PDDL action name, lifted context, primitive body step, and add effects, so ambiguous primitive bindings are auditable instead of reported only as a count. | Done |
| T120 | Align architecture contract with action-candidate diagnostics. | G1, G5 | The machine-readable architecture contract now states that feature-binding diagnostics include action-candidate details for ambiguous primitive effect bindings, keeping synthesis reports aligned with the current G5 implementation. | Done |
| T121 | Explain missing external rule bindings. | G5, G6 | `ExternalRuleBindingReport` now includes structured diagnostics for each missing learned-policy condition/effect binding, including the DLPlan expression, available bound operators, rejection reason, and action-candidate details. | Done |

## Current Completion Rule

Before claiming a gap is complete:

1. Add or update tests that fail before the change.
2. Implement the smallest domain-agnostic change that satisfies the test.
3. Run the relevant test suite.
4. Update this file with the new status and evidence.
5. Commit and push the milestone.
