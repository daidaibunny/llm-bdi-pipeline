# GP2PL 论文瘦身：14 页 → 7 页

## Context

**为什么做**：论文投 AAAI-27，[官方 CFP](https://aaai.org/conference/aaai/aaai-27/main-technical-track-call/) 规定**正文 7 页**（8–9 页只能放参考文献）。本地编译实测 **18 页**（正文到 p14 + References p15–16 + Checklist p17–18）。**正文须砍一半。**

**为什么现在做**：实验已全部冻结（`paper_artifacts/` + 5 seed + 1228 TEG 全过），项目从"跑实验"转入"写论文"。用户定位是审稿 + 简单修改，不碰主线实验。

**目标产出**：分支 `review-paper-xiaoyi`，正文压到 7 页且**不重跑任何实验**、不改任何数字。

**用户已授权**（本轮 AskUserQuestion）：
1. ✅ 可**同步修改** `tests/test_aaai_manuscript_layout.py` 的锁定断言
2. ✅ 可**修改生成器脚本**（`scripts/freeze_paired_ablation_results.py` 等）

---

## 关键约束（已实测，非推断）

| 事实 | 数据 | 来源 |
|---|---|---|
| 页密度 | **~880 词/页**（p3=886, p4=801, p6=880, p8=948） | `pdftotext` 逐页 |
| **跨栏 float 7 个** | **≈3.5 页** — 页数最大头 | `grep -c 'begin{table\*\|figure\*}'` |
| 正文词数 | 8,777（Method 4,361 = 53%） | `wc -w` |
| **真实预算** | 7p − floats 2.55p = **正文 4.45p ≈ 3,900 词 → 须砍 ~4,900 词** | |
| TR 现状 | **18 页 / 10,487 词，无页数限制** | 可接收删下的内容 |

### ⚠️ `tests/test_aaai_manuscript_layout.py` 是内容锁定文件

**改 .tex 必须同 commit 改它**，否则测试挂。锁定项：

| 断言 | 行 | 影响 |
|---|---|---|
| `tab:supported-fragment` / `tab:compiler-obligations` **必须在 method.tex** | `:270-271` | **挡住 Table 1/2 移 TR** |
| float placement 必须 `htbp`；`t!` 精确计数各 1 | `:54-60` | 增删 float 须同步改计数 |
| 禁 `\clearpage` `\newpage` `\tiny` `\scriptsize` | `:61-70` | 不能靠缩字号省页 |
| caption 在最后 `\end{tabular}` 之后，label 在 caption 之后 | `:114-115` | 改 caption 须守序 |
| Fig 1/2 caption **逐词正则匹配** | `:475-491`, `:536-547` | **不能动这两个 caption** |
| method.tex 锁死数十个公式串（`\mathfrak G`, `lexargmin`, `\mathcal D_q=`…） | `:553-583`, `:649-716` | 删小节须同步解锁 |
| Algorithm 1 的 `\ENSURE` 串锁在 method.tex | `:461` | Alg 1 不能移 |
| result 表主/副分工锁定 | `:272-281` | `五seed_domain_table`/`domain_table` 必须在 TR |

### result_*.tex 是**生成产物**

头部 `% Auto-generated by scripts/...`。手改不 fail 测试（生成器测试全在 `tmp_path` 里跑，无 CI），**但下次跑生成器会静默覆盖** → **必须改生成器**：

| 文件 | 生成器 |
|---|---|
| `result_atomic_comparison_table.tex`, `result_temporal_comparison_table.tex` | `scripts/freeze_paired_ablation_results.py:48-56` |
| `result_profile_table.tex`, `result_domain_table.tex` | `scripts/generate_evaluation_tables.py:501-518` |

---

## 执行顺序

### Step 0 — 开分支 + 落地计划
```bash
git checkout -b review-paper-xiaoyi          # 从干净的 main@921289eb
cp <scratchpad>/PLAN.md docs/paper_revision_plan.md
```
基线：`latexmk main.tex` → 记录 18 页；`pytest tests/test_aaai_manuscript_layout.py` → 记录绿。

### Step 1 — 提拔主线（+100w）★先做
**诊断**：Method 4,361 词**没有一句讲"为什么用证明视角"**，读者拿不到主线 → 显得"信息堆叠"。
**但主线已存在**，只是埋错地方（冷审发现）：
- `method.tex:72-74` —— 埋在 **Table 2 的 caption 里**
- `method.tex:177` — "Every candidate carries a machine-checkable certificate."
- `main.tex:192`（Conclusion）— "Unproved obligations fail compilation rather than becoming unchecked plans."
- **`main.tex:132-134` — 全文 thesis 句**（"temporal requests extend one maintained library instead of triggering a new domain-level learning run"）

**动作**：把 caption 里那句**提到 §4.1 开头**；补 4 个"所以"连接词：
| 位置 | 补什么 |
|---|---|
| §4.1→4.2 | evidence 只覆盖见过的目标，**所以**闭合注定不成立——除非自己造候选 |
| §4.2→4.3 | 自造候选**不保证**对，**所以**每条要过证书 |
| §4.3→4.4 | 闭合是**集合级**性质，**所以**必须整体优化而非贪心 |
| §4.4→4.5 | M_D 与 query 无关，**所以**只需追加不必重编 |

🔴 **`main.tex:132-134` 必须保留**（v1 误判为"复述图"要删）。

### Step 2 — float 减负（~1.5p）★页数最大头
| 动作 | 需同步改 |
|---|---|
| **Table 1 / Table 2 → TR**（两张全是纯文本句子，零数字，是"排版成表格的散文"） | **解锁 `test:270-271`** |
| Table 6 external → TR | — |
| Table 2 若留则**跨栏改单栏**（保 `[htbp]`） | float 计数 |
| **Algorithm 2 → TR**（同事第5条已定） | `test:517-519` |
| 增删 float 后 | **同步 `t!` 精确计数 `:59-60`** |

### Step 3 — 表的列与 caption（改生成器）
**实测 Table 4**：
```
Method            Compiled  Targets  Valid       Branches      KiB          Compile s
Evidence Only     80/80     90/365   5420/6140   835.0 ± 15.8  488.3 ± 8.3  5.9 ± 0.7
Direct Producers  80/80     365/365  5419/6140   1153.0 ± 15.8 549.2 ± 8.3  8.0 ± 2.5
Maximum Feasible  80/80     365/365  6059/6140   1533.0 ± 15.8 645.9 ± 8.3  41.5 ± 13.1
Full GP2PL        80/80     365/365  6059/6140   1499.0 ± 15.8 635.1 ± 8.3  40.7 ± 7.6
```
| 列 | 问题 | 动作 |
|---|---|---|
| `Compiled` | **四行全 `80/80`** = 零信息 | **删列**，正文一句带过 |
| `Targets` | 后三行全 `365/365` | 删列（正文已有这句） |
| `Compile s` | 同事第7条**不提慢** | **删列** → 不讨论就不占列 |
| `Valid` | `5420/6140` 难比较 | 改 **`88.3% (5420)`** —— 一眼看出 10.42pp |
| `± 15.8`/`± 8.3` | **四行完全相同**（是跨 seed 非跨方法） | caption 须说清，否则读者以为巧合 |
| Table 5 `Built` | 四行全 `1228/1228` | 删列 |

**7 列 → 4 列 ⟹ 可从跨栏改单栏**（再省 0.25p）。
**caption 瘦身**：Table 4 = **97 词**（比表体还长！）、Table 5 = 83 词 → 各压到 ~30 词，**配置统一在 §5 开头讲一次**（同事第12条）。
⚠️ 保 `\resultbest`/`\resultselected`（`test_generate_aaai_comparison_tables.py:1307-1310` 锁定）。
⚠️ **删列不改任何数字**，完整数据仍在 TR。**不重跑实验。**

### Step 4 — 删真重复（~1,400w）
已逐项 diff 核实为**逐字重复**，TR 已有更全版本：
| 内容 | 位置 | TR |
|---|---|---|
| Φ_syn 语法块 | `background.tex:68-74` | `:171-177` **字符级相同** |
| Σ_b 投影 | `method.tex:177-181` | `:257-265`（TR 更全） |
| `Closed_D` ∀∃ 公式 / mode graph `G_b^res` / `κ_S` | method.tex | TR §1.3 |
| `ρ_num` / `I_q_s` / `W_q_s` 公式 | method.tex | TR `:857-866` |
| Prop 4 / Prop 6 | method.tex | TR |

### Step 5 — 5 项"部分重复"**压成一句**（非删除，~1,100w）
冷审核实：公式重复但**正文有 TR 没有的直觉句**。🔴 **这三句必须留正文**：
| 保留 | 位置 | 为什么 |
|---|---|---|
| "controlled utterances 只是评测分布，**不是强制用户语法**" | `method.tex:365-367` | **正面防守审稿人最可能的攻击**，TR 里没有 |
| "Our certificates apply to this trigger–context–body model…" | `background.tex:50-51` | 全部证书 claim 的**范围锚点**，TR 里没有 |
| "两次出现需不同 portfolio" | `method.tex:458-460` | per-occurrence 索引的**唯一解释**，TR 里没有 |

其余压缩：参数化输入 448w → ~60w；Portfolios 154w → 1 句；branch 五元组删公式留锚点句。

### Step 6 — §4.5 Temporal 大砍（1,900 → 450w）
**留**：M_D 与 query 无关 ⟹ 只追加；DFA progress 边 ⟹ 签名义务 `O_χ`；threat 图无环⟹拓扑序/有环⟹portfolio/joint/**证不出⟹拒绝**；monitor 每个 primitive action 后求值（Prop 5 前提 + 9.28pp 来源）。
**移 TR**：参数化输入、Algorithm 2、Σ_b、Portfolios 展开、MONA cube 辩护。

**Numeric 保留 ~150w**（同事第10条：**必须讲清楚咋实现的**）：
- 留：常量 delta 动作（单位 delta 方向严格 / 大 delta 精确前驱）、混合 guard 要求一次完整终态证明、**4 个 numeric domain 建立在这上面**
- 🔴 **留 `at(P,L) ∧ fuel(V)=3` 例子** — TR 里**没有**，是这节价值所在
- 移 TR §2.3：`ρ_num` / `I_q_s` / `W_q_s` 公式

**Balanced ~3 句**（同事第5条措辞已定，**防止重跑代码**）：
> Both encodings execute the same certified literal order. Linear serialization places all obligations in one plan body that grows with guard size, whereas balanced dispatch groups contiguous obligations into binary helper plans, keeping every generated plan body constant-sized. Certified serialization, not the dispatch shape, provides the temporal guarantee.

放弃 "必然 timeout/stack overflow"（未证明）；Balanced 是所有实验的实际配置，**不能从方法里拿掉**。

**Maintained Domain Library**（同事第8条：只出现一次，吸收精华）：
已核实 `L_D^[k]` 在 `background.tex:130-137` 和 `method.tex:590-612` **各一次**（正文内部重复，非与 TR 重复）。**留 background 版**，吸收 method 版的 "Appending Q_q neither reconstructs nor reoptimizes the atomic core"；noop 论证随 Prop 6 → TR 并留一句指向。

### Step 7 — §4.3 改名 + §2/§3 压缩（~890w）
**§4.3 "Per-Branch Certificates" → "Progress by Well-Founded Ranking"**（同事第6条：这里能写数学证明，是统一视角）：
> 凡可能无限绕圈的结构，都配一个严格递减到底的量。**ℕ 上不存在无穷下降**（TR Lemma 3 原话）。

| 证书 | 递减量 | TR |
|---|---|---|
| relational recursion | `ρ_b: States→ℕ` | **Lemma 3** |
| ranked repair | `κ_M` | **Lemma 3** |
| resource discharge | 有限非重复 mode path | **Lemma 4** |
| numeric | `ρ_num` | §2.3 |

🔴 **resource discharge + ranked repair 保留**（同事第6条）—— 它们是 **Lemma 3/4，Prop 1 的承重墙**（TR `:116`: "Lemma 3 excludes infinite selected recursion, and Lemma 4…"）。

**§2 Background** 996→380w；**§3 Related Work** 511→240w（NL 那段 → TR）。
⚠️ v1 说"每段都以 instead 收尾"是**编造** —— 实测 5 段仅 2 处。论点（正交≠更好）仍成立，改法：至少一段说"比谁强"。

### Step 8 — Evaluation 重排（零成本纯收益）
**Paired Ablations（10.42pp / p=1.47e-39 / PAR-2 40倍）提到最前** —— 全文最硬证据，现被埋在 Table 3 之后。
🔴 **Table 3 MOOSE source split 保留正文**（v1 要移，冷审反对且我认可：**头牌消融打平时（6059=6059），evidence 来源分区正是怀疑者要捅的地方，移走=自拆防线**）。

### Step 9 — Clingo + Full 定位
**Full vs MaxFeasible 定为结构性贡献**（用户已授权我定）：
- 理由：与同事第7条自洽；选项 a 需新数据而**项目已冻结**；**诚实** —— Table 4 自己会拆穿虚假 claim
- **Clingo**（同事第7条）：说**干什么**（从证书通过的候选选最小可行核）+ **优势**（压缩空间）+ **为什么必须是求解器**（闭合是集合级性质）；**不提慢**
- 🔴 **须留一行字典序目标陈述**，否则 **Prop 2 悬空**（冷审发现的依赖）

### Step 10 — Figure 3 重绘 —— ⏸ **本轮不做（用户指令：排除绘图相关）**
Figure 1（最终稿）、Figure 2（同事在催画）、Figure 3（重绘）全部属于绘图，**本轮跳过**。
仅保留一条**非绘图**的结论供记录：表的 bold/蓝色高亮已达标（`\resultbest` + `\resultselected` + "Color is not used alone"），不需要动。
Figure 3 的重绘（参照 MOOSE Fig 3 的 ECDF 累积覆盖曲线）留到内容定稿后单独一轮处理。

---

## 关键文件

| 文件 | 动作 |
|---|---|
| `latex_code/aamas_method_paper/sections/method.tex` | 4,361 → ~1,400w ★主战场 |
| `sections/evaluation.tex` | 重排 + 1,845 → ~1,100w |
| `sections/background.tex`, `related_work.tex` | 996→380, 511→240 |
| `main.tex` | Intro 压缩（**保 `:132-134` thesis 句**） |
| `sections/technical_appendix_content.tex` | 接收所有删下内容 |
| **`tests/test_aaai_manuscript_layout.py`** | **同步解锁被改的断言** |
| **`scripts/freeze_paired_ablation_results.py`** | 改表的列 + caption |
| `scripts/generate_evaluation_tables.py` | 同上 |

## 不做的事
- ❌ **本轮不做任何绘图**（用户指令：排除绘图相关 —— Figure 1/2/3 全部不碰，Figure 3 重绘留到内容定稿后单独一轮）
- ❌ 不重跑任何实验（同事第5条明确要求）
- ❌ 不改任何数字（删列只是不展示，完整数据在 TR）
- ❌ 不碰 `paper_artifacts/`（冻结产物）
- ❌ 不 push、不 merge（本地分支，供审阅）

## 本轮覆盖的核心问题（用户 13 点，排除绘图）
| 点 | 内容 | 落在 |
|---|---|---|
| 1 | 压到 7 页 | 全局 |
| 3(内容部分) | 表内容展示优化（**排版/可视化留到绘图轮**） | Step 3 |
| 4 | 删 JSON/工程用语 | Step 5 |
| 5 | Balanced 只提两优势 + Algo/ablation → TR | Step 6 |
| 6 | resource discharge + ranked repair 保留（数学证明） | Step 7 |
| 7 | Clingo 往好了写 | Step 9 |
| 8 | Maintained Library 只出现一次 | Step 6 |
| 9 | Σ_b + Portfolios → TR | Step 4/5 |
| 10 | Numeric 保留且讲清实现 | Step 6 |
| 11 | 纯文本表 → 散文或 TR | Step 2 |
| 12 | caption 配置统一移 TR | Step 3 |
| 13(内容部分) | 删表的零信息列 | Step 3 |
| **2** | **Figure（全部绘图）** | **⏸ 本轮跳过** |

## Verification

```bash
# 1. 页数（唯一的硬指标）
cd latex_code/aamas_method_paper && latexmk main.tex
python3 -c "import re,subprocess;d=open('build/main.pdf','rb').read();print('Pages:',max(int(m) for m in re.findall(rb'/Count\s+(\d+)',d)))"
# 逐页定位 References 起点 —— 正文必须 ≤ p7
for p in 6 7 8 9; do pdftotext -f $p -l $p build/main.pdf - | head -2; done

# 2. 测试必须绿（改了 .tex 就必须同步改它）
/opt/miniconda3/envs/llm-bdi/bin/python -m pytest tests/test_aaai_manuscript_layout.py -q

# 3. 生成器一致性（改了生成器要重跑，确认 .tex 不被覆盖回去）
/opt/miniconda3/envs/llm-bdi/bin/python -m pytest tests/test_freeze_paired_ablation_results.py tests/test_generate_evaluation_tables.py -q

# 4. TR 仍能编译（接收了删下的内容）
latexmk technical_appendix.tex

# 5. 数字未被篡改（冻结产物不变）
git diff --stat main -- paper_artifacts/   # 必须为空
```

**验收标准**：正文 ≤7 页 · 全部测试绿 · `paper_artifacts/` 零改动 · 三句"必须留正文"的话仍在 · Prop 2 不悬空。

## 风险
| 风险 | 缓解 |
|---|---|
| 改测试锁定串 = 碰主线 | 用户已授权；每步单独 commit，可回滚 |
| 手改 result_*.tex 被生成器覆盖 | **改生成器**（用户已授权），不手改 |
| 砍到 Prop 依赖的内容 | Step 4/5 前先理清 Prop 2/4/6 依赖链 |
| 4,900w 砍不到位 | 备用：Table 2 → TR、Algorithm 1 → TR、§2 并入 §1 |
