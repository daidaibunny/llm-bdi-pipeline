\section{Experimental Evaluation}
\label{sec:evaluation}

Figure~\ref{fig:evaluation-summary} summarizes the complete five-seed held-out
matrix and its predeclared benchmark groups; the estimands, validation oracles,
and timing boundary are defined below.

\IfFileExists{\gpplfigurethreepath}{%
\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{\gpplfigurethreepath}
\caption{Five-seed held-out execution. Panel (a) gives per-domain
Jason-plus-VAL coverage for five independently seeded MOOSE evidence runs;
points are seeds and diamonds with bars show mean $\pm$ sample
standard deviation. Panel (b) gives mean cumulative VAL-valid coverage over
Jason execution time for three predeclared benchmark groups; bands span the
seed minimum and maximum. Failures remain in the denominator, and the dashed
line marks the per-instance time limit.}
\label{fig:evaluation-summary}
\end{figure*}
}{%
\PackageWarning{gp2pl}{Five-seed result figure unavailable}%
}

\subsection{Questions and Comparisons}

\paragraph{Atomic compilation.}
Evidence Only validates provider macros against the PDDL schemas and
retains them without producible-target expansion. Direct Producers adds
action-only producers for the producible target universe; Maximum Feasible
retains a maximum-cardinality jointly feasible subset after adding
compatible recursive precondition-repair and target-preserving
resource-discharge candidates; and Full GP2PL applies the lexicographic
Clingo objective to the same generated certified set. Their paired
comparison measures target coverage, internal-call closure, held-out execution,
and compact selection of the atomic core.

\paragraph{Temporal composition.}
Unprotected Serialization uses the same automaton, library, and
primitive-step monitor without completion-effect analysis. Certified Flat adds
threat-induced precedence ordering and preservation portfolios; Certified
Balanced changes only flat control to the balanced binary transition-repair tree. A
Module-Return Monitor observes only on module return,
testing the primitive-step observation boundary.

\paragraph{End-to-end references.}
End-to-end evaluation measures valid traces and amortized compilation. Raw
MOOSE, LAMA, ENHSP, and FOND4LTLf with LAMA
\cite{Chen2026Moose,RichterWestphal2010LAMA,
Scala2020NumericSubgoaling,DeGiacomoFuggitti2021FOND4LTLf} are task-level
references, not compiler ablations. The MOOSE comparison is partitioned by
provenance before scoring. For all twelve domains in its original benchmark, we
report the five-model mean coverage from Table~4 of the extended paper
\cite{Chen2025MooseExtended}. For all four GP2PL-added domains, we measure Raw
MOOSE locally over seeds 0--4. We never select a source separately for an
individual domain after observing its outcome.

\subsection{Benchmarks and Protocol}

\paragraph{Corpus and repetitions.}
The corpus comprises eight classical and four bounded-integer MOOSE domains with
official splits \cite{Chen2026Moose,DillonZChenMooseDataset}, plus four
serialized-width families \cite{LipovetzkyGeffner2012Width}. Blocksworld Clear
and On use published no-constants splits
\cite{Frances2021LearningGeneralPolicies,
BonetLearnerPoliciesRepository}; Tower uses the first quarter of a fixed IPC
corpus \cite{Bacchus2001AIPS00,PotasscoPDDLInstances}, and Depots the first half
of the 22-instance D2L corpus
\cite{Bonet2018Features,RLeapD2LRepository}. Groups are used only for reporting.
Following MOOSE \cite{Chen2026Moose}, the atomic study uses all training
instances and three goal orders to compile five independently seeded atomic
cores. Source revisions, split construction, resource limits, and reporting
rules appear in the Technical Supplement, Secs.~4.3 and~5.2--5.3.

\paragraph{Temporal benchmark construction.}
For each held-out problem, benchmark construction ignores the original
achievement goal and derives candidate temporal milestones from legal,
state-changing, nonrepeating rollouts of at most three actions; it invokes no
planner. Predicate and bounded-integer changes instantiate five predeclared
profiles: $F(A\land B)$, $F(A\land\neg B)$, $F(A\land X(F(B)))$,
$F(A\land X(F(B\land X(F(C)))))$, and strong $A\,U\,B$.
For example, a witness that first establishes \texttt{holding(b1)} and later
\texttt{on(b1,b4)} yields the lifted formula
$F(\mathit{holding}(X)\land X(F(\mathit{on}(X,Y))))$ under a typed hidden
binding.

Each candidate retains a legal witness. Its objects are lifted to typed
parameters, and selection balances profile use and alpha-normalized
semantic-signature use before applying a symbol-independent tie-break. The
resulting benchmark contains one witness-backed query for each of the 1,228
test problems. Requests with identical translation inputs and agreeing hidden
semantics are translated once, yielding 475 distinct translation inputs while
retaining all 1,228 queries for execution. The complete construction and
registered bounds appear in the Technical Supplement, Sec.~4.1.

\paragraph{Controlled inputs and references.}
Atomic variants share each seed's evidence; temporal variants are paired on the
same atomic core, binding, and DFA. MOOSE reuses its learned model; LAMA
\cite{RichterWestphal2010LAMA} and ENHSP MRP+HJ
\cite{Scala2020NumericSubgoaling} are the classical and numeric references used
by MOOSE \cite{Chen2026Moose}. FOND4LTLf
\cite{DeGiacomoFuggitti2021FOND4LTLf} with LAMA covers its accepted grounded
Boolean subset.
The published MOOSE row is coverage-only: it is labelled Reported rather than
locally reproduced, and its time and memory are not compared across hardware.
The local Raw MOOSE extension row is labelled Measured and reports mean
$\pm$ sample standard deviation over five independently trained models. The
original and added scopes contain 1,080 and 148 cases per seed, respectively.

Table~\ref{tab:moose-reference} fixes this source split and its observed
coverage. Across the four added domains, Raw MOOSE obtains
\RawMooseExtensionMeanPercent{}\% $\pm$ \RawMooseExtensionSDPercent{}
percentage points over five independently trained models. Its consistently low
coverage marks the limit of direct application to the added serialized-width
families. Because the Reported and Measured rows cover disjoint domain scopes,
their difference is not a same-instance performance comparison; the same-scope
GP2PL result appears separately in Table~\ref{tab:five-seed-atomic}.

\input{sections/result_moose_reference_table}

\paragraph{Validation and measures.}
A temporal success requires Jason completion \cite{Bordini2007Jason}, VAL
accepts the full primitive trace against a neutral PDDL goal
\cite{Howey2004VAL}, and independent replay is accepted by both DFAs. Atomic
validation instead uses the original PDDL goal. We report internal-call closure,
held-out success, library size, runtime, action count, and trigger fan-out; plan
length uses jointly solved instances. Figure~\ref{fig:evaluation-summary}
follows MOOSE's absolute-time presentation \cite{Chen2026Moose}: coverage is
credited only to VAL-valid traces, and all failures remain in the denominator.
Tables~\ref{tab:moose-reference}, \ref{tab:five-seed-atomic}, and
\ref{tab:profile-results} report the MOOSE source split, compact atomic result,
and temporal-profile outcomes. The Technical Supplement preserves every
domain--seed cell, diagnostic, and fixed temporal input. The result subsections
interpret these predeclared views without selecting domains after execution.

\IfFileExists{sections/result_external_reference_table.tex}{%
\input{sections/result_external_reference_table}
}{%
\PackageWarning{gp2pl}{External reference table unavailable}%
}

\paragraph{External-reference outcomes.}
LAMA validates \ExternalLAMAValidCount{}/\ExternalLAMACaseCount{} classical
achievement instances, while MRP+HJ validates
\ExternalMRPHJValidCount{}/\ExternalMRPHJCaseCount{} numeric instances. Their
PAR-2 values are \ExternalLAMAParTwoSeconds{} and
\ExternalMRPHJParTwoSeconds{} seconds, respectively. These rows cover disjoint
planning fragments and are therefore interpreted within scope rather than as a
same-instance ranking. FOND4LTLf with LAMA supports
\ExternalDirectSupportedCount{} of the \ExternalDirectTotalCaseCount{} temporal
queries and validates \ExternalDirectValidCount{}; the remaining
\ExternalDirectUnsupportedCount{} inputs are reported as unsupported rather
than planner failures. Its supported-set PAR-2 is
\ExternalDirectParTwoSeconds{} seconds. Raw MOOSE extension runtime remains
omitted under its registered coverage-only protocol.

\subsection{Five-Seed Atomic Results}
\label{sec:atomic-results}

Across five independently compiled atomic cores, mean held-out coverage is
\AtomicFiveSeedMeanSuccessPercent{}\% $\pm$
\AtomicFiveSeedSampleSDPercent{} percentage points. Fourteen of the sixteen
domains are complete. Figure~\ref{fig:evaluation-summary} includes every domain
and the three predeclared groups rather than a post-hoc subset.

Variation is concentrated in Logistics and Depots. In Logistics, seeded goal
order changes the available cross-mode provider macros; action-schema-derived
generation cannot replace every missing macro because the relevant producer
dependency is cyclic and lacks a decreasing certificate. Depots instead
requires a nested support-resource choice beyond the current bound-capacity
certificate. These failures delineate the present candidate-construction
grammar rather than invalidating successful traces, all of which satisfy the
original-goal VAL criterion. No domain-specific rule was introduced after
observing held-out outcomes.
Table~\ref{tab:five-seed-atomic} gives the non-pooled five-seed summary; the
Technical Supplement, Sec.~5.3 gives every domain--seed count, provenance, and
failure diagnostic.

\input{sections/result_five_seed_atomic_table}

\subsection{End-to-End Temporal Results}
\label{sec:end-to-end-results}

The temporal protocol exposes the public PDDL vocabulary, typed parameters, and
binding constraints; it admits $F$, $X$, strong $U$, conjunction, and literal
negation. The five registered structures instantiate
$\Phi_{\mathrm{bench}}$, rather than exhausting the recursive syntax
$\Phi_{\mathrm{syn}}$; execution results include only queries admitted to
$\Phi_{\mathrm{cert}}(D,\mathcal M_D)$. The controlled source utterances specify
the evaluation distribution, not a required user grammar. GPT-5.5
\cite{OpenAI2026GPT55SystemCard}
translates \TranslationInputCount{} deduplicated specifications. Every accepted
translation satisfies the typed structural validation conditions and exact
finite-trace language equivalence, yielding \TEGProblemCount{} bound queries.
The translation and validation protocol appears in the Technical Supplement,
Sec.~4.2.

Table~\ref{tab:profile-results} separates five formula profiles. All
\TEGProblemCount{} bound queries satisfy the end-to-end criterion: Jason
completes, all \TEGVALSuccessCount{} nonempty traces pass VAL, and both DFAs
accept all \TEGGoldDFAAcceptCount{} traces. Runtime is
\TEGMedianRuntimeSeconds{} seconds median (interquartile range 4.28--8.49), and
action count is \TEGMedianActionCount{} median (interquartile range 1--2).

\input{sections/result_profile_table}

The temporal study fixes one preregistered seed-0 atomic core per domain,
separately from the five-seed sensitivity study; it supports neither
cross-seed claims nor arbitrary PDDL--\ltlf{} completeness. The Technical
Supplement, Sec.~5.3 gives the semantic-conformance cases, fixed-input domain
breakdown, complete distributions, and provenance.

\subsection{Paired Ablation Results}
\label{sec:paired-ablation-results}

\input{sections/result_atomic_comparison_table}

All four atomic variants compile all 80 seed--domain libraries. Evidence Only
reaches \AtomicEvidenceOnlyCoveredTargetCount{}/365 producible predicates,
whereas every schema-expanded variant reaches 365/365. Direct Producers differs
from Evidence Only on one execution, reflecting AgentSpeak plan choice rather
than an infrastructure failure. Adding the certified recursive candidate
families in Maximum Feasible yields
\AtomicDirectToMaximumValidGain{} additional valid executions over Direct
Producers; their exact paired binary test gives
$p=\AtomicDirectToMaximumExactP{}$. Full GP2PL preserves the same successful
case set as Maximum Feasible while removing \AtomicFullBranchReduction{} mean
branches and \AtomicFullLibraryKiBReduction{} mean KiB, isolating compact
selection without a coverage loss.

\input{sections/result_temporal_comparison_table}

Completion-effect ordering and preservation portfolios produce a net gain of
\TemporalUnprotectedToFlatValidGain{} valid traces over Unprotected
Serialization; \TemporalUnprotectedToFlatRightOnlyValidCount{} cases favor
Certified Flat and \TemporalUnprotectedToFlatLeftOnlyValidCount{} favors the
unprotected controller ($p=\TemporalUnprotectedToFlatExactP{}$, exact paired
binary test). Certified Flat and Certified Balanced have the same
\TemporalCertifiedBalancedValidCount{}-case success set.
Balancing therefore receives no semantic credit: it changes median controller
plans from 7 to 13 and maximum transition-repair fan-out from
\TemporalCertifiedFlatFanout{} to \TemporalCertifiedBalancedFanout{} while
retaining coverage. Moving observation from primitive steps to module return
loses \TemporalBalancedToModuleReturnLeftOnlyValidCount{} valid traces;
Module-Return Monitor records
\TemporalModuleReturnJasonTimeoutCount{} Jason timeouts,
\TemporalModuleReturnJasonFailedCount{} Jason failure, and
\TemporalModuleReturnGoldDFARejectedCount{} gold-DFA rejection. The shared
Certified Flat/Balanced failure,
\texttt{logistics\_p0\_24}, is a genuine strategy-choice boundary: the selected
loading branch leaves the monitor in its source state.

The separate fixed-library end-to-end run above remains
\TEGProblemCount{}/\TEGProblemCount{}. The paired matrix conditions on the
seed-0 Full GP2PL library emitted within its own run, so its Certified Balanced
row is the ablation estimate rather than a replacement for that fixed-input
result.
