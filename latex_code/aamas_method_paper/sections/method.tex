\section{Certified Policy Lifting into a Reusable Atomic Core}
\label{sec:method}

\subsection{Certification Boundaries}

GP2PL separates deterministic input validation from two compiler certification
stages. Atomic compilation first checks each candidate branch locally through
$\operatorname{Cert}_D(b)$ and records its completion summary $\Sigma_b$. It
then checks candidate sets globally for evidence and schema coverage,
internal-call closure, and compatible progress and dependency ranks before
selecting $\mathcal M_D$. Query compilation reuses the selected summaries to
construct a transition certificate recording an occurrence-specific preservation
portfolio $\boldsymbol\Pi_\chi$ and serialization $\boldsymbol\ell_\chi$ for each
DFA guard $\chi$; it neither
reopens atomic selection nor relearns $\mathcal M_D$.

These obligations are checked before rendering, which preserves each selected
trigger, context, ordered body, and internal-call edge; the Technical Supplement
formalizes the constructors and accept/reject boundary, proves the resulting
soundness and monitor claims, and gives compiler pseudocode (Secs.~1--3).

\subsection{Evidence Normalization and Canonical Lifting}

Figure~\ref{fig:compiler-stages}(a) summarizes the query-independent path from
raw evidence through canonical lifting and certified candidate selection to
$\mathcal M_D$.
An evidence provider is admissible when its raw artifact
$\mathcal E_{\mathrm{raw}}$ parses to a singleton-goal evidence program $E_0$.
Canonical lifting produces
$E=\operatorname{Lift}_D(E_0)$, whose rules pair a partial state condition and
one goal atom with a PDDL action macro, numeric effects, and provenance.
Every symbol and arity must occur in $D$. Lifting capture-avoidingly
alpha-normalizes provider variables, preserves repeated-variable sharing, and
keeps constants declared by $D$ fixed, so
\texttt{on(block0,block1)} becomes \texttt{on(X,Y)}. It normalizes a policy
already generalized by the provider rather than inferring one from ground
plans. Atomic evidence is restricted to positive singleton achievements;
negative literals remain guard conditions and do not induce synthetic atomic
goals.

\subsection{Action-Schema Candidate Generation}

Evidence need not cover the signatures required by internal calls. Candidate
generation therefore expands from evidence goals to schema-producible targets,
independently of which goals the provider observed.
Let $\operatorname{goalPred}(E)$ contain the predicate symbols targeted by
positive predicate evidence and $\operatorname{prod}(D)$ the predicates
occurring in some action's add effects; the domain-wide producible target
universe is
\[
T_D(E)=\operatorname{goalPred}(E)\cup\operatorname{prod}(D).
\]
Every add-effect predicate thus receives candidate atomic modules. Predicates
with neither validated evidence nor a positive producer do not; static and
delete-only predicates remain non-achievement context. Supported numeric
equalities form $T_D^{\mathbb Z}(E)$, the admitted singleton integer-equality
targets in $E$.

The finite grammar $\mathfrak G$ contains six constructors: producer,
regression, recursion, precondition repair, resource discharge, and numeric
progress. For Boolean and numeric targets, $\textsc{LiftSchemas}$ computes
$\mathcal C_D(E)=
\operatorname{Inst}_D(\mathfrak G,T_D(E),T_D^{\mathbb Z}(E))$,
using only typed schemas and effects from $D$, then alpha-normalizes each
variable-level branch.
For target set $T$, the producer--precondition graph
$\mathcal G_D^{\mathrm{prod}}(T)$ has an edge $p\rightarrow r$ when a producer
for $p$ has a positive dynamic precondition with predicate $r$.
Backward STRIPS regression
\cite{Fikes1972GeneralizedRobotPlans,Alcazar2013Regression,Chen2026Moose}
on an acyclic reachable graph first unifies compatible open requirements,
introduces fresh variables only for unbound parameters, and rejects conflicting
deletes. An alpha-normalized regression state records open requirements,
producer role, and resource modes; search stops before repeating such a state
and keeps the shortest body for an equivalent completion summary. Construction
is therefore finite without an arbitrary action-depth constant; validated
evidence macros are never truncated. Symbolic progression verifies every
candidate, while cyclic dependencies require provider evidence or a separately
certified recursive module. Let $\mathcal C_E$ be the validated evidence
branches. The resulting candidate set
$\mathcal C_{D,E}=\mathcal C_E\cup\mathcal C_D(E)$ is finite, but
$\mathfrak G$ is not a complete hypothesis language for PDDL planning. Types
constrain unification; rendered sort membership is context-only.

\subsection{Candidate Certification}

Symbolic progression proves preconditions and target achievement and yields each
candidate's conditional module-completion summary $\Sigma_b$, with Boolean
projections $\mustadd_b$, $\mayadd_b$, and $\maydelete_b$, any exact
constant-integer delta, and a keyed resource-mode transition. The $\mustadd_b$
facts hold after every feasible completion; $\mayadd_b$ and $\maydelete_b$
overapproximate possible effects. Let $\operatorname{Cert}_D(b)$ be the
branch-certification predicate: it holds exactly when typed binding, symbolic
executability, target achievement, and every applicable constructor-specific
progress or resource condition have valid witnesses. The selected core must
additionally satisfy the set-level condition
$\operatorname{Closed}_D(\mathcal M_D)$; the definitions follow.

\paragraph{Binding, symbolic execution, and internal-call closure.}
Action and subgoal variables must be bound by the trigger, positive context, or
validated static sort membership; negative contexts and comparisons only
filter.
$\operatorname{Closed}_D(\mathcal R)$ holds when every internal call in plan set
$\mathcal R$ type-unifies with a selected trigger under $D$'s type hierarchy.
Requiring $\operatorname{Closed}_D(\mathcal M_D)$ is a set-level Clingo
obligation: it proves call resolution, not universal branch applicability.

\paragraph{Well-founded recursive progress.}
A same-predicate recursive branch $b$ requires a ranking function
$\rho_b:\operatorname{States}(D)\rightarrow\mathbb N$, either a non-negative
global relation count or an anchor-local count
$\rho_{R,x}(s)=|\{y\mid R(y,x)\in s\}|$ that strictly decreases under a
reachable-state acyclicity assumption, together with an
already-satisfied, direct-producer, or validated-evidence terminal branch.
Acyclicity prevents recursive calls from returning to an earlier anchor.
Threats are checked over the preparation graph selected by Clingo; a branch may
add the ranked relation at another anchor only under a guard proving that the
new anchor differs from the protected one. The ranking is a compile-time
witness, not an agent belief, and claims only generated schema-certified
features. The construction follows relational progress witnesses in
general-policy work \cite{Frances2021LearningGeneralPolicies}.

\paragraph{Target-preserving resource discharge.}
Temporary occupation of a reusable resource is identified from action effects:
when an acquisition action deletes an availability fact
$\mathit{free}(R)$ and adds an occupancy relation $\mathit{held}(R,U)$ (names
illustrative), the compiler infers the resource and occupant arguments from the
effect structure and forms a finite graph of alpha-normalized keyed occupancy
modes. A discharge body is accepted only if symbolic execution removes the
current occupancy mode, restores the availability fact, leaves the atomic
target true, and does not recreate an earlier normalized mode; excluding
repeated modes makes the search finite without assuming that release is one
action.

\paragraph{Ranked cross-module precondition repair.}
Preparing one of several dynamic preconditions may temporarily change another,
such as an object's location. A repair branch may defer an unrelated sibling
only through a schema-inferred single-valued fluent when every deferred sibling
has one producer, all static feasibility witnesses remain, and the original
target is retried before primitive production. For a candidate core
$\mathcal M$, Clingo assigns each trigger signature in its selected call graph
a dependency rank $\kappa_{\mathcal M}$ that strictly decreases along every
selected call edge; same-predicate recursion instead requires the relational
ranking witness above.

\subsection{Feasible-Core Optimization}

Validated evidence macros and certified action-schema-derived candidates enter
one answer-set optimization problem solved with Clingo
\cite{Gebser2018Clingo}. Evidence rules induce coverage obligations
$\operatorname{covers}_D(b,e)$. Coverage requires alpha-equivalent triggers
and either an equivalent body and summary, the same body under a weaker
context, or a producer refinement with the same $\mustadd$, no
additional $\maydelete$, and equivalent numeric and keyed-resource summaries.
For each nonrecursive schema branch, $\operatorname{realizes}_D(b,c)$ permits
an equivalent branch, a refinement with no weaker completion contract, or a
target-preserving resource-discharge alternative with the same achievement
contract. Body-prefix similarity alone satisfies neither relation.
Define
\begin{align*}
\mathcal C^{\checkmark}_{D,E}
  &=\{b\in\mathcal C_{D,E}\mid\operatorname{Cert}_D(b)\},\\
\mathfrak F_{D,E}
  &=\{\mathcal M\subseteq\mathcal C^{\checkmark}_{D,E}\mid
      \mathcal M\models\Omega_{D,E}\land
      \operatorname{Closed}_D(\mathcal M)\},
\end{align*}
where $\Omega_{D,E}$ contains evidence coverage, schema-achievement coverage,
preparation acyclicity, and recursive-ranking compatibility, and resource
discharge is part of the per-branch predicate $\operatorname{Cert}_D$.
The optimizer uses the
lexicographic objective
\begin{multline*}
\mathcal M_D=\operatorname*{lexargmin}_{\mathcal M\in\mathfrak F_{D,E}}
\bigl\langle-N_{\mathrm{rel}}(\mathcal M),-N_{\mathrm{prep}}(\mathcal M),
|\mathcal M|,\\
N_C(\mathcal M),N_\beta(\mathcal M)\bigr\rangle,
\end{multline*}
where $N_{\mathrm{rel}}$ and $N_{\mathrm{prep}}$ count compatible
relation-ranked recursion and acyclic precondition-repair capabilities,
$N_C$ counts context literals, and $N_\beta$ counts body steps. The selected
trigger--context--body rules, rather than their AgentSpeak rendering, define
$\mathcal M_D$; the claim is global only within
$\mathcal C^{\checkmark}_{D,E}$, not over ungenerated programs.

Figure~\ref{fig:atomic-case} isolates one compositional derivation through
certified branches in $\mathcal M_D$. Under the displayed binding, branch $b_0$
expands
$\texttt{!on(y,z)}$ into $\texttt{!clear(y)}$ followed by a retry without
changing the PDDL state; $b_1$ clears $y$, and $b_2$ establishes
$\texttt{on(y,z)}$. Several selected branches may apply in one state, so the
figure shows an available derivation rather than an executor tie-breaking rule.

\begin{figure}[htbp]
\centering
\IfFileExists{\gpplatomiccasepath}{%
\includegraphics[width=\columnwidth]{\gpplatomiccasepath}%
}{%
\fbox{\parbox[c][1.55in][c]{0.92\columnwidth}{\centering
Atomic-core execution placeholder\\[5pt]
$\texttt{on(z,y),on(y,x)}$\\
$\rightarrow\ !\texttt{clear(y)}\rightarrow\ !\texttt{on(y,z)}$}}
}
\caption{A derivation through a certified Blocks World atomic-core slice:
internal subgoal expansion followed by schema-action execution.}
\label{fig:atomic-case}
\end{figure}

\section{Typed Query Translation and Preservation-Safe Goal Composition}
\label{sec:temporal-composition}

\subsection{Controlled-Language Translation}

A frozen language-model prompt receives one controlled-language request, its
declared typed parameters and constraints, and the public PDDL catalogue. It
returns a fixed eight-key JSON object containing an unbound parametric \ltlf{}
query formula and a one-to-one proposition table. A formula without $F$, $X$, or
$U$ is an ordinary achievement query; a formula using one of these operators is
a TEG. Deterministic validation rejects
undeclared symbols, wrong arities or types, unsupported operators, incomplete
atom tables, and altered parameter constraints; it neither grounds parameters
nor selects actions or objects. Invocation later supplies $\theta_q$. For
evaluation only, a hidden construction formula permits exact finite-trace
language equivalence; this gold oracle is not an online input requirement. The
complete prompt and translation protocol appear in the Technical Supplement,
Sec.~4.

Figure~\ref{fig:compiler-stages}(b) summarizes the query-specific path from a
bound query and its DFA progress guard to a certified transition-repair tree and
appended query plans.

\paragraph{Achievement completion embedding.}
Let $\varphi_q$ be the bound query formula. If
$\varphi_q\in\Phi_{\mathrm{ach}}$, GP2PL compiles $F\varphi_q$ and returns when
the DFA first accepts, so the controller ends satisfying
$\varphi_q$; if $\varphi_q$ is a TEG, it is compiled unchanged. Both cases then
pass through the same DFA construction stage, progress-edge certification,
monitor, and transition-repair controller. The embedding is therefore not a
second wrapper or a bypass of the DFA path.

\paragraph{Progress-edge compilation.}
Given the validated bound query $\widehat\tau_q$ and DFA $\mathcal D_q$ of its
execution formula from Sec.~\ref{sec:background}, GP2PL compiles one controller
for each progress edge
$(q_s,\chi,q_t)$; an accepting \texttt{true} self-loop needs none. The external
binding $\theta_q$ instantiates query propositions while the shared atomic core
$\mathcal M_D$ remains lifted. For signed obligation
$\mathcal O_\chi=\langle P_\chi^+,P_\chi^-\rangle$, compilation derives a
serialization $\boldsymbol\ell_\chi$ and occurrence-specific preservation
portfolios $\boldsymbol\Pi_\chi$ from the selected completion summaries. The
transition certificate records the selected branches, confirmation that their
summaries are complete, the induced threat relation, and the checked order. If
this certificate cannot be constructed, compilation rejects the progress edge;
otherwise $\boldsymbol\ell_\chi$ is rendered as the balanced binary
transition-repair tree of Sec.~\ref{sec:balanced-trees}.

\subsection{Summaries and Preservation Portfolios}

A relational fixed point over selected calls computes each branch summary
$\Sigma_b$, following the definite/potential effect distinction in BDI
goal-interference analysis \cite{Thangarajah2003GoalInterference}. For positive
occurrence $G_i\in P_\chi^+$, a preservation portfolio
$\Pi_{\chi,i}\subseteq\mathcal M_D$ contains the certified branches allowed to
establish $G_i$ under the obligations protected at that occurrence. Portfolios
are occurrence-specific: two occurrences of the same predicate may admit
different branches.

A negative guard $\neg N$ is observed absent or discharged by an exact
certified deleter; it never becomes a synthetic $!\neg N$ goal. A portfolio
branch is admissible only when its conditional $\mayadd$ excludes $N$. For a
chosen portfolio tuple, the
threat-induced precedence graph
$\mathcal G_\chi=(P_\chi^+,\prec_\chi)$ places $G_j$ before $G_i$ when a feasible
$\maydelete$ of $\Pi_{\chi,j}$ may delete $G_i$. An acyclic graph yields a
topological serialization. If no portfolio choice removes a cycle, compilation
requires a joint branch $b_\chi^{\mathrm{joint}}$ whose complete net effect
establishes the entire guard, is callable from every positive occurrence it
establishes, and retains every query variable; otherwise it rejects the edge.

\subsection{Numeric and Joint-Guard Certification}

Numeric obligations reuse the same certification machinery. A positive
integer equality is achieved by a complete constant-delta action: a unit delta
requires strict direction and a larger delta the exact predecessor value. A
mixed Boolean--numeric guard uses complete action-only net effects; for example,
a body for $\mathit{at}(P,L)\land \mathit{fuel}(V)=3$ must symbolically establish
$\mathit{at}(P,L)$ and the exact integer value $3$ in its final state while
preserving every other signed obligation. Negated equality is observation-only
without a certified change-away action. Numeric preparation uses
$\rho_{\mathrm{num}}=\langle n_{\mathrm{miss}},d_{\mathrm{target}}\rangle$,
ordering missing preconditions before bounded target distance. Strong-Until
source invariants are the signed literals common to every non-progress
self-loop guard and are preserved until progress.

\subsection{Balanced Binary Transition-Repair Trees}
\label{sec:balanced-trees}

For query $q$, let $\mathcal R_{q_s,\chi}$ denote the plans for one source-state
transition obligation; the query-local plan set $\mathcal Q_q$ contains the
top-level dispatcher together with every accepted $\mathcal R_{q_s,\chi}$, one
per progress obligation. Each $\mathcal R_{q_s,\chi}$ realizes
$\boldsymbol\ell_\chi$ by recursively splitting contiguous literal ranges at
their midpoint. Internal nodes dispatch two child ranges, leaves observe or
repair one signed literal, and the completion plan retries only while the
monitor remains at $q_s$. The transition certificate, rather than the tree
shape, provides the temporal guarantee. Trigger fan-out denotes the number of sibling
AgentSpeak plans sharing one transition-repair trigger. Flat control therefore
has fan-out $O(n)$, whereas a linear body gives an $O(n)$-long intention
continuation; balanced dispatch preserves the certified order while bounding
both local quantities by two.

Appending $\mathcal Q_q$ leaves $\mathcal M_D$ unchanged. Query-local goals are
control symbols; only primitive PDDL actions change the observed state.

\section{Conditional Guarantees}
\label{sec:guarantees}

These guarantees apply only to accepted artifacts; each proposition states its
branch-local, candidate-space, or supported-composition boundary.

\paragraph{Atomic branch soundness.}
\textbf{Proposition 1.} If an emitted $p(\bar X)$ branch's context holds under a
type-consistent grounding and each called module satisfies the achievement
contract and completion summary established by its certification, the branch
is executable and returns with $p(\bar X)$. Binding grounds each term; induction
over symbolic progression establishes preconditions and final achievement;
recursion uses its ranking and preparation rechecks the producer context.
\hfill$\square$ The proposition establishes local soundness, not
reachable-state coverage.

\paragraph{Certified-candidate-set optimality.}

\textbf{Proposition 2.} For the grounded selection instance defined above, a
Clingo optimum $\mathcal M_D$ belongs to $\mathfrak F_{D,E}$ and minimizes the
declared lexicographic cost over that feasible family. The result does not imply
complete candidate generation or optimality over all AgentSpeak programs.

\paragraph{Supported transition-composition soundness.}

\textbf{Proposition 3.} Let $\mathcal O_\chi=\langle
P_\chi^+,P_\chi^-\rangle$ be an accepted signed transition obligation. If each
required signed repair has an applicable certified branch,
each selected call terminates, and later repairs preserve earlier obligations,
then at the end of a complete transition-repair pass the controller retries
exactly when the monitor reports the source state; otherwise it returns to
dispatch at the
current state. Every intervening state change is an actual DFA edge enabled by
the observed valuation, and a rejecting state cannot report success.

\paragraph{Proof sketch.}
Induction over the certified order shows that each leaf
establishes its signed literal and later summaries preserve prior positives and
absences; if a call leaves the source, the remaining certified suffix preserves
this prefix at return boundaries. Because only the monitor evaluates full cubes
after every primitive action, every observed source exit is a real DFA
transition. After a complete pass, completion retries exactly while the source
remains active. \hfill$\square$ This does not guarantee that a post-exit suffix
avoids rejection.

\paragraph{Structural and monitor-fidelity guarantees.}
The balanced tree for $n$ signed literals and $e$ repair plans has
$2n+e+2$ plans, fan-out and body length at most two, and depth $O(\log n)$
while preserving the certified order (Proposition~4). After every successful
primitive action, the runtime monitor state equals the deterministic automaton
state over the observed finite trace, so leaving a controller source state is
exactly a DFA exit and controller plans cannot write monitor beliefs
(Proposition~5). If the initial valuation is accepting, execution
commits no primitive action and denotes the one-state trace
$\langle s_0\rangle$ rather than a noop, which could change
strong-Next or Until truth (Proposition~6).
