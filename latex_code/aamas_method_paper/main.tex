% File: main.tex
\documentclass[letterpaper]{article}
\usepackage[submission]{aaai2027}
\usepackage[hyphens]{url}
\usepackage{graphicx}
\urlstyle{rm}
\def\UrlFont{\rm}
\usepackage{natbib}
\usepackage{caption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{algorithm}
\usepackage{algorithmic}

\frenchspacing
\pdfinfo{/TemplateVersion (2027.1)}
\setcounter{secnumdepth}{2}

\title{GP2PL: From Generalized Planning Evidence to Certified BDI Plan
Libraries for Temporally Extended Goals}
\author{Anonymous Submission}
\affiliations{}

\newcommand{\pddl}{Planning Domain Definition Language}
\newcommand{\asl}{AgentSpeak(L)}
\newcommand{\ltlf}{\ensuremath{\mathrm{LTL}_f}}
\newcommand{\mustadd}{\ensuremath{\mathrm{MustAdd}}}
\newcommand{\maydelete}{\ensuremath{\mathrm{MayDelete}}}
\newcommand{\mayadd}{\ensuremath{\mathrm{MayAdd}}}
\newcommand{\methodname}{GP2PL}
\newcommand{\resultbest}[1]{\textbf{#1}}
\newcommand{\resultselected}[1]{\textbf{#1}}
\newcommand{\selectedmark}{\textsuperscript{\ensuremath{\dagger}}}
\providecommand{\gpplfigureonepath}{figures/fig1_architecture.png}
\providecommand{\gpplfiguretwopath}{figures/fig2_compiler_stages.pdf}
\providecommand{\gpplatomiccasepath}{figures/fig3_atomic_case.png}
\newif\ifgpplpublicartifact
% Keep false for anonymous review; set true only in the camera-ready source.
\gpplpublicartifactfalse
\newcommand{\gpplartifacturl}{https://github.com/daidaibunny/gp2pl}

\input{sections/result_macros}
\input{sections/result_five_seed_atomic_macros}
\input{sections/result_moose_reference_macros}
\input{sections/result_external_reference_macros}
\input{sections/result_comparison_macros}

\begin{document}
\maketitle

\begin{abstract}
Generalized planning learns reusable behavior for problem families, whereas
Belief--Desire--Intention (BDI) agents execute plan libraries. Direct adaptation
leaves a representation gap: singleton-goal evidence may omit modules for
subsidiary achievements and does not certify temporal composition. We introduce
\methodname{} (Generalized Planning to Plan Libraries), which compiles such
evidence and Planning Domain Definition Language (PDDL) action models into
certified BDI plan libraries. GP2PL validates and lifts evidence, generates
schema-derived modules for producible goals, selects a compact closed
atomic core, and appends query controllers guided by deterministic finite
automata for finite-trace temporal goals without relearning that core. For the
generated candidate language and supported fragment, accepted branches and
controllers satisfy conditional soundness guarantees; uncertified obligations
are rejected. Using five fixed evidence seeds and one atomic core per domain and
seed, mean held-out coverage over \BenchmarkDomainCount{} domains is
\AtomicFiveSeedMeanSuccessPercent{}\%
($\AtomicFiveSeedSampleSDPercent{}$ percentage-point sample standard deviation).
Recursive candidates improve paired atomic success by 10.42 percentage points,
and preservation certification improves paired temporal success by 9.36
percentage points. All \TranslationEquivalentCount{} translated specifications
match their gold finite-trace languages, and the fixed-core temporal study
produces independently validated accepting traces for all \TEGProblemCount{}
admitted bound queries.
\end{abstract}

\section{Introduction}

Classical PDDL planning computes an action sequence for one grounded problem
\cite{McDermott1998PDDL}; generalized planning learns reusable behavior for a
family of related problems
\cite{Srivastava2011DirectedSearch,Chen2026Moose}. A
Belief--Desire--Intention (BDI) interpreter uses a different representation: a
plan library that connects events and belief contexts to actions and subsidiary
goals \cite{Rao1996AgentSpeak,MeneguzziDeSilva2015PlanningBDI}. Turning a
generalized policy into such a library is therefore a compilation problem, not
a change of syntax.

In Blocks World, a learned rule for the lifted achievement $\texttt{on(X,Y)}$
may use the action \texttt{stack(X,Y)} when the robot is holding $X$ and the
destination block is clear. If $Y$ is not clear, however, an executable BDI
library also needs a reusable plan for making it clear, even when that condition
never appeared as a training goal. More generally, variables must remain safely
bound, subsidiary goals must resolve to library plans, and recursive plans must
make progress. Figure~\ref{fig:architecture} summarizes the representation
bridge and its query-reuse boundary.

\begin{figure}[htbp]
\centering
\IfFileExists{\gpplfigureonepath}{%
\includegraphics[width=\columnwidth]{\gpplfigureonepath}%
}{%
\fbox{\parbox[c][1.40in][c]{0.92\columnwidth}{\centering
Figure 1 artwork placeholder\\[5pt]
Single-column GP2PL overview\\
Domain compilation $+$ query-specific temporal compilation}}
}
\caption{GP2PL separates reusable domain compilation from query-specific
temporal compilation. An external generalized-planning backend derives raw
singleton-goal evidence $\mathcal E_{\mathrm{raw}}$ from domain $D$ and training
instances $\mathcal I_{\mathrm{train}}$. Validated policy lifting compiles these
inputs once into $\mathcal M_D=\mathcal L_D^{[0]}$; DFA-guided temporal
compilation maps each bound query $\widehat{\tau}_q$ to query-local plans
$\mathcal Q_q$, updating the sole maintained library by
$\mathcal L_D^{[k+1]}=\mathcal L_D^{[k]}\cup\mathcal Q_q$.}
\label{fig:architecture}
\end{figure}

Temporally extended goals add a second representation gap
\cite{DeGiacomoVardi2013,DeGiacomoRubin2018AutomataFOND}: a request to place one
block on another and only later clear a third constrains the whole finite
trace, and a conjunctive milestone must not be undone by a later plan---a form
of BDI goal interference \cite{Thangarajah2003GoalInterference}. Safe reuse
therefore requires temporal control over the fixed domain library and
observation after primitive actions.

\begin{figure*}[t!]
\centering
\IfFileExists{\gpplfiguretwopath}{%
\includegraphics[width=\textwidth]{\gpplfiguretwopath}%
}{%
\fbox{\parbox[c][2.75in][c]{0.96\textwidth}{\centering
Figure 2 artwork placeholder\\[8pt]
(a) Query-independent atomic-core compiler\qquad
(b) Bound query $\rightarrow$ DFA guard $\rightarrow$ certified repair tree}}
}
\caption{Inside the two GP2PL compiler stages. The query-independent stage
normalizes and lifts evidence, constructs and certifies schema-derived
candidates, and selects $\mathcal M_D=\mathcal L_D^{[0]}$. For the bound Blocks
World tower query, the query-specific stage reuses selected completion summaries
to certify a preservation portfolio $\boldsymbol\Pi_\chi$ and serialization
$\boldsymbol\ell_\chi$ for its MONA-derived conjunctive progress guard, then
renders the certified order as a balanced transition-repair tree.}
\label{fig:compiler-stages}
\end{figure*}

GP2PL makes three contributions: it compiles singleton-goal evidence and PDDL
schemas into an internally closed BDI core with modules for omitted producible
subgoals; it accepts only branches carrying certificates for binding, symbolic
execution, achievement, completion effects, and progress; and it appends
preservation-safe finite-trace controllers over the fixed core, monitoring DFA
progress after each primitive action.

We evaluate component and end-to-end behavior only within the declared
candidate and formula fragments; formal definitions, proofs, and the reporting
protocol appear in the Technical Supplement.

\input{sections/background}
\input{sections/related_work}
\input{sections/method}
\input{sections/evaluation}

\FloatBarrier
\section{Conclusion and Future Work}

GP2PL compiles generalized-planning evidence and typed PDDL schemas into a
closed atomic core, then appends preservation-safe DFA controllers to one
maintained BDI library; uncertified obligations fail closed.

Using five fixed evidence seeds and one atomic core per domain and seed, mean
held-out coverage over \BenchmarkDomainCount{} domains was
\AtomicFiveSeedMeanSuccessPercent{}\%
(\AtomicFiveSeedSampleSDPercent{}-point sample SD), while recursive candidates
improved paired atomic success by 10.42 percentage points. Preservation
certification improved paired temporal success by 9.36 points; all
\TranslationEquivalentCount{} translations matched their gold finite-trace
languages, and all \TEGProblemCount{} admitted queries produced validated accepting
traces. Certification improved both reusable coverage and
temporally correct execution.

GP2PL does not claim arbitrary PDDL--\ltlf{} strategy synthesis: it rejects
arithmetic outside the bounded-integer fragment, formulae outside the declared
$F$, $X$, $U$, conjunction, and literal-negation fragment, and uncertified
recursive or interference repair. Future work should
broaden providers, candidate grammars, and temporal fragments under fail-closed
certification, with pinned PDDL generators and held-out-disjoint instances for
domain onboarding.

\ifgpplpublicartifact
\paragraph{Artifact availability.}
Code, proofs, fixed result artifacts, and the versioned GP2PL temporal-goal
dataset are available at \url{\gpplartifacturl}.
\fi

\bibliography{references}

\def\isChecklistMainFile{}
\input{sections/reproducibility_checklist}

\end{document}
